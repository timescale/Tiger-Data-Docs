# AGENTS.md

Single source of truth for AI coding agents working in this repository (Claude Code, Cursor, Warp, and others). `CLAUDE.md` imports this file, so keep all agent guidance here.

## Project overview

Tiger Data Docs is a documentation site built with **Astro 6 + Starlight + Stainless Docs** (`@stainless-api/docs`). It covers Tiger Cloud and TimescaleDB (including self-hosted and Managed Service for TimescaleDB). Deployed on Vercel.

**Node requirement:** >=22.12.0. Uses pnpm.

## Commands

```bash
pnpm dev              # Start dev server at localhost:4321
pnpm dev:local        # Dev server without Stainless (DOCS_LOCAL_WITHOUT_STAINLESS=1)
pnpm build            # Production build (Astro syncs content types as part of the build)
pnpm build:local      # Production build without Stainless
pnpm preview          # Preview production build
pnpm format           # Prettier formatting
pnpm lint:prose       # Vale prose lint on changed *.md/*.mdx (pnpm lint:prose -- --all for everything)
pnpm lint:links       # Build with link checking (CHECK_LINKS=true)
```

**Stainless auth:** generating the API reference requires `stl auth login` or `STAINLESS_API_KEY` in `.env`. The `dev:local` / `build:local` variants skip Stainless for faster local work.

## Architecture

### Content

- **MDX docs** live in `src/content/docs/`, organized into seven top-level sections (also the site's tabs): `get-started`, `learn`, `build`, `migrate`, `integrate`, `reference`, `deploy`.
- **Reference content** under `reference/`: `timescaledb/` (SQL function reference), `toolkit/` (Toolkit function reference), and `configuration/` (PostgreSQL/TimescaleDB settings). These are hand-maintained in this repo; keep them matching the product's actual API and version (for example, verify Toolkit signatures and stability against the product source). The Tiger Cloud REST API reference is auto-generated by the `stainlessDocs()` integration from the `tiger-cloud` Stainless project and nested under the Reference tab; it has no source directory in the repo, so don't hand-edit it. The only checked-in REST file is `reference/tiger-cloud-rest-local-preview.mdx`, a placeholder shown when you run without Stainless (`DOCS_LOCAL_WITHOUT_STAINLESS=1`).
- **Partials** (`src/partials/_*.mdx`): 300+ reusable MDX snippets imported to reduce duplication. Partials use an underscore prefix.
- **Sidebar navigation and tabs** are defined in `astro.config.ts` (large file, ~2,000 lines), the authoritative source for nav and tab structure. For redirect handling, see [Redirects](#redirects).

### Constants system (critical)

All product/brand names are centralized in `src/constants.ts`. Import as:

```ts
import * as C from "@constants";
```

Then use `{C.CLOUD_LONG}`, `{C.PG}`, `{C.TIMESCALE_DB}`, and so on in MDX content. The `TigerData.ProductConstants` Vale rule nudges literals toward their constant. Exceptions: inside backticks, URLs, frontmatter, and image alts. Treat `src/constants.ts` as the source of truth for available constants.

### TypeScript path aliases

- `@components/*` → `src/components/*`
- `@constants` → `src/constants.ts`
- `@partials/*` → `src/partials/*`

### Custom components

Key overrides and components in `src/components/`:

- `Callout.astro`: custom styled callout. A Vite alias points `@stainless-api/docs/components` at `src/lib/docs-components.ts`, which re-exports the Stainless components with this `Callout` (and `Button`) swapped in, so MDX importing `Callout` from the Stainless package gets ours.
- `Header.astro`, `Footer.astro`, `PageTitle.astro`, `PageNavigation.astro`: Starlight overrides.
- `PageSidebar.astro` + `LearnMore.astro`: right-rail "Learn more" card driven by per-page frontmatter (`learnMore:` block). Auto-hides when absent. Full authoring guide: [`src/components/LearnMore.README.md`](./src/components/LearnMore.README.md). Prefer this over one-off "related content" patterns.
- `NumberedList.astro` / `NumberedList.tsx` / `NumberedItem`: ordered step components for procedures.
- `AuthorByline.astro`, `SinceRelease.astro`, `ChangelogEntry.astro` (plus `ChangelogFilter`/`ChangelogTag`), `Glossary/`, `IntegrateOverview.astro`, `IntegrateToc/`, `BuildToc/`, `HeaderSearchBar.astro`.
- React `.tsx` components for interactive pieces: `CopyToClipboard`, `Prerequisites`.

### Custom plugins

- `src/plugins/remark-resolve-constants-in-headings.ts`: resolves `{C.CONSTANT}` syntax in headings.
- `src/plugins/rehype-base-path.ts`: adds `BASE_PATH` to relative URLs.
- `src/plugins/rehype-pagefind-weight.ts`: applies the per-page `products` frontmatter as a Pagefind search weight (cloud-only pages rank higher).

### Content schema

Defined in `src/content.config.ts`. Extends Starlight's `docsSchema` with custom fields:

- `pageLabels`: tags like `Experimental`, `Popular feature`, `Optional`.
- `overviewDescription`: dropdown descriptions.
- `integrationSchema`: metadata for the Integrate section.
- `learnMoreSchema`: the right-rail card (see [Custom components](#custom-components)).
- `seoDescription` (optional): a summary for SEO and social cards that doesn't affect the sidebar subtitle. If both `description` and `seoDescription` are set, `description` wins for meta and sidebar. Implemented in `src/components/Head.astro`.

### Theming

`theme.css` defines CSS custom properties (accent, background) and uses the CSS `light-dark()` function for light/dark switching. Additional tokens in `src/styles/`.

## Authoring a page

### Where pages live

New pages are MDX files under `src/content/docs/<section>/`, where `<section>` is one of the seven tabs. The sidebar is defined explicitly in `astro.config.ts`: add a new page to its section's sidebar entries so it appears in the nav. 

### Frontmatter

Every page starts with YAML frontmatter:

```yaml
---
title: Page title in sentence case
description: One sentence used for the sidebar subtitle, search results, and meta description.
products: [cloud, self_hosted]   # which products the page applies to
keywords: [topic, technology]    # optional, improves search
---
```

- `title` is required. `description` is strongly recommended.
- `products` accepts `cloud`, `mst`, and `self_hosted`. It weights Pagefind search (cloud-only pages rank higher); it does not hide or gate page content.
- Frontmatter is plain YAML, so it can't use `{C.X}` constants. Write product names out here (for example `PostgreSQL`, not `{C.PG}`).
- Other optional fields (`seoDescription`, `pageLabels`, `learnMore`, integration metadata) are defined in `src/content.config.ts`.

### Components

The full component reference, with copy-paste examples, is [`README-component.md`](./README-component.md) (callouts, buttons, partials, Prerequisites, and more). Import only what you use. Common sources:

```mdx
import * as C from "@constants";
import { Callout } from "@stainless-api/docs/components";
import { Tabs, TabItem } from "@astrojs/starlight/components";
import { NumberedList, NumberedItem } from "@components/NumberedList";
import { Prerequisites } from "@components/Prerequisites";
```

Two things that trip people up: `Callout` variants are only `tip`, `note`, `important`, `warning`, and `callout` (a call-to-action with a button), with no `info`/`success`/`danger`/`caution`; and `Tabs`/`TabItem` come from `@astrojs/starlight/components`, not the Stainless package. Use `NumberedList` and `NumberedItem` for step-by-step procedures.

### Partials

Reusable snippets live in `src/partials/_name.mdx` (underscore prefix). Import and reuse them instead of duplicating content shared across pages. Most pages import partials with a relative path (the depth depends on the page's location; the `@partials` alias also resolves):

```mdx
import ConnectionPooling from "../../../partials/_connection-pooling.mdx";
```

### Images

Put first-party images in `src/assets/images/` and reference them with an `import` so Astro optimizes them (astro:assets, Sharp). Use `public/` only when you need a raw static URL with no processing. See `src/assets/images/README.md`.

### Links

Use root-absolute internal links of the form `/section/page`, with no `.mdx` extension and no trailing slash (for example `/get-started/quickstart/create-service`). `starlight-links-validator` checks links during `pnpm lint:links`.

### SQL in examples

- Use the current columnstore/hypercore API names, never the deprecated compression API (see [Content conventions](#content-conventions)).
- To create a hypertable, prefer the `CREATE TABLE ... WITH` syntax. Only use `create_hypertable()` when you need to convert a table that already exists:

  ```sql
  -- Recommended: create the hypertable directly
  CREATE TABLE metrics (
    time   timestamptz NOT NULL,
    device text,
    value  float
  ) WITH (tsdb.hypertable, tsdb.partition_column = 'time');

  -- Only when the table already exists as a regular table
  SELECT create_hypertable('metrics', by_range('time'));
  ```

### Before you finish

- Run `pnpm build` to compile every page (it fails on MDX or component errors).
- Run `pnpm lint:prose` to check changed files against the style rules (requires Vale locally: `brew install vale`).
- When you introduce a new term, add a glossary entry in `src/lib/glossary-data.ts`.
- Task-specific authoring skills live in `.claude/skills/` (for example, writing a tutorial or an integration guide). Use them when they fit.

## Content conventions

House prose style follows the Google developer documentation style guide, enforced by Vale (`.vale.ini`), plus the TigerData-specific rules below. Accepted product/brand/feature spellings live in the `TigerData` vocabulary (`.github/styles/config/vocabularies/TigerData/accept.txt`). The human-facing version of this guidance lives in `src/content/docs/get-started/contributing.mdx` (published at `/get-started/contributing`).

- **Use constants for product names.** `TigerData.ProductConstants` nudges literal product/brand names toward their constant (suggestion). It covers only product/brand-name constants (the `// General` and `// Products` sections of `src/constants.ts`). Feature/service/project common-nouns (hypertable, chunk, service, job, columnstore, and the like) are accepted in the Vale vocabulary instead. The rule is hand-maintained at `.github/styles/TigerData/ProductConstants.yml`. When you add or rename a product/brand name in `constants.ts`, update the swap list there too. Enforced terms must never be added to the Vale vocabulary, or the nudge is globally suppressed.
- **No em dashes.** `TigerData.NoEmDash` flags em dashes in prose (warning); rewrite the sentence. Prefer commas, semicolons, colons, parentheses, or two sentences. For label-and-description lines, use a colon: `**Label**: description`.
- **UI elements in code font, not bold.** `TigerData.UIElementsCodeFont` flags `**Bold**` UI labels after action verbs (warning).
- **Partials use underscore prefix:** `_partial-name.mdx`.
- **No deprecated compression APIs.** TimescaleDB 2.18.0 renamed the compression API to columnstore/hypercore (`compress_chunk` to `convert_to_columnstore`, `*_compression_policy` to `*_columnstore_policy`, `timescaledb.compress` to `timescaledb.enable_columnstore`, and so on). Old names still work as backwards-compat aliases but must not appear in new docs. `TigerData.CompressionAPIs` enforces this (gating error, scans SQL too). Full mapping, carve-outs, and verification grep: [`.claude/references/deprecated-compression-apis.md`](./.claude/references/deprecated-compression-apis.md).

## CI checks

- **vale.yml**: lints prose against the Google style guide plus custom `TigerData` rules. Only the lines a PR changes are checked, and only errors gate. `TigerData.CompressionAPIs` is the only rule set to error (everything else is advisory). Failures are explained in the run's step summary plus inline annotations. See `.github/scripts/vale_pr_lint.py`.
- **pr-checklist-check.yml**: ensures PR checklist items are addressed.
- **affected-pages.yml**: posts preview links for changed pages on successful deploy.

## Pull requests

- If the repository has a PR template (`.github/pull_request_template.md`), fill out each section rather than replacing or overriding it.
- Issue templates live in `.github/ISSUE_TEMPLATE/` (changelog request, doc correction or change, doc feedback or bug). The changelog-request template points contributors at [`README-changelog.md`](./README-changelog.md).

## Redirects

Redirects live in two places, and which one to edit depends on where the old URL comes from:

- **`astro.config.ts`** (`redirects: withBase({ ... })`) is the in-app redirect map, applied by Astro at build time. Use it when you **move or rename a page within this repo** (for example, content shuffled between `learn/` and `build/`). Keys are site-relative with **no `/docs` prefix and no trailing slash** (for example `"/api": "/reference/tiger-cloud-rest"`); the `withBase()` helper prepends the deploy base path to destinations because Astro doesn't do it automatically. Don't define both trailing-slash and non-trailing-slash variants of a key, as that hard-errors the build.
- **`vercel.json`** (`redirects` array) is the platform/edge redirect layer, applied by Vercel **before** the app reaches Astro. In production the site is served under `/docs`, so these keys **include the `/docs` prefix**. Use it for **legacy external URLs** from the old `docs.timescale.com` site (`/docs/use-timescale/latest/...`, `/docs/api/latest/...`, `/docs/tutorials/latest/...`), the root `/` to `/docs`, and anything that must redirect off-site. Each entry sets `permanent` (`true` = 308, `false` = 307), and keys can use params (`:slug`, `:path*`). `vercel.json` also owns trailing-slash normalization (`trailingSlash: false`), the `rewrites` that serve `index.md` to AI and markdown clients, and cache `headers`.

Rule of thumb: an internal page move is an Astro redirect; preserving an old published `/docs/...` URL is a Vercel redirect.

## Hosting, search, and cache

The site builds with `pnpm build` and can deploy anywhere that serves static files (Vercel, Netlify, Cloudflare Pages).

- **Search**: Pagefind by default (header search bar and ⌘K). For Algolia, set `PUBLIC_ALGOLIA_APP_ID`, `PUBLIC_ALGOLIA_INDEX`, `PUBLIC_ALGOLIA_SEARCH_KEY`, `PRIVATE_ALGOLIA_WRITE_KEY` (see `.env.example`).
- **Cache headers**: set per [Stainless' recommendation](https://www.stainless.com/docs/docs-platform/hosting-and-deploys/#configuring-cache-headers). Use long-lived caching for `/_astro/*` (versioned assets) and a shorter TTL for HTML. This repo configures them via `public/_headers` (Netlify, Cloudflare Pages) and `vercel.json` (Vercel).

## Environment

Requires `STAINLESS_API_KEY` for API reference generation (see `.env.example`). Optional Algolia env vars for search (above).

## Reference docs

Deeper, authoritative guides in the repo. Prefer these over re-deriving conventions:

- [`README.md`](./README.md): development quickstart, running the site locally, and where content lives.
- [`README-component.md`](./README-component.md): full component usage guide (callouts, buttons, partials, Prerequisites) with copy-paste examples.
- [`src/components/LearnMore.README.md`](./src/components/LearnMore.README.md): the per-page right-rail "Learn more" card.
- [`.claude/references/deprecated-compression-apis.md`](./.claude/references/deprecated-compression-apis.md): the deprecated-to-current compression API mapping.
- `src/content/docs/get-started/contributing.mdx`: the human-facing contributor and writing-style guide.
- [`.github/templates/template-tutorial.mdx`](./.github/templates/template-tutorial.mdx): starting-point MDX for a new tutorial (copy into a content directory). The `write-docs-tutorial` skill builds on it.
- [`README-statsig.md`](./README-statsig.md): the Statsig analytics integration (inline script in `astro.config.ts` between the `BEGIN STATSIG`/`END STATSIG` markers, `PUBLIC_STATSIG_CLIENT_KEY` env var), and the checklist to remove it. Read this before touching the `head[]` block or analytics env vars.
- **Changelog**: add an entry by editing `src/content/docs/get-started/news/new.mdx`, following the structure in [`README-changelog.md`](./README-changelog.md).