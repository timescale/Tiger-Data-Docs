import * as C from "@constants";

1. Update the table statistics.

    ```bash
    psql $TARGET -c "ANALYZE;"
    ```

1. Verify the data in the target {C.SERVICE_LONG}.

   Check that your data is correct, and returns the results that you expect,

1. Enable any {C.CLOUD_LONG} features you want to use.

   Migration from {C.PG} moves the data only. Now manually enable {C.CLOUD_LONG} features like
   [hypertables](/use-timescale/hypertables/), [hypercore](/use-timescale/hypercore/) or [data retention](/use-timescale/data-retention/about-data-retention/)
   while your database is offline.

1. Reconfigure your app to use the target database, then restart it.
