import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";

<NumberedList>
<NumberedItem title="Update the table statistics">

```bash
psql $TARGET -c "ANALYZE;"
```
</NumberedItem>

<NumberedItem title={`Verify the data in the target ${C.SERVICE_LONG}`}>
Check that your data is correct, and returns the results that you expect.
</NumberedItem>

<NumberedItem title={`Enable any ${C.CLOUD_LONG} features you want to use`}>
Migration from {C.PG} moves the data only. Now manually enable {C.CLOUD_LONG} features like
[{C.HYPERTABLE}s](/build/data-management/understand-hypertables), [{C.HYPERCORE}](/learn/columnar-storage) or [data retention](/learn/data-management/data-retention)
while your database is offline.
</NumberedItem>

<NumberedItem title="Reconfigure your app to use the target database, then restart it">
</NumberedItem>
</NumberedList>
