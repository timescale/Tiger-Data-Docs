import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";

### Restoring data into a {C.SERVICE_LONG} with COPY

<NumberedList>

<NumberedItem title={`Connect to your ${C.SERVICE_LONG}:`}>

```sql
psql "postgres://tsdbadmin:<PASSWORD>@<HOST>:<PORT>/tsdb?sslmode=require"
```

</NumberedItem>

<NumberedItem title={`Restore the data to your ${C.SERVICE_LONG}:`}>

```sql
\copy <TABLE_NAME> FROM '<TABLE_NAME>.csv' WITH (FORMAT CSV);
```

Repeat for each table and hypertable you want to migrate.

</NumberedItem>

</NumberedList>
