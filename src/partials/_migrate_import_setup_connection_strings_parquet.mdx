import * as C from "@constants";

This variable holds the connection information for the target {C.SERVICE_LONG}. 

In the terminal on the source machine, set the following:

```bash
export TARGET=postgres://tsdbadmin:<PASSWORD>@<HOST>:<PORT>/tsdb?sslmode=require
```
See where to [find your connection details](/integrations/find-connection-details/).

