import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";
import TestPlan from "@components/TestPlan.astro";

You can use client credentials to programmatically access resources instead
of using your username and password. You can generate multiple client
credentials for different applications or use cases rather than a single set of
user credentials for everything.

## Create client credentials

When you create client credentials, a public key and a private key are generated.
These keys act as the username and password for programmatic client
applications. It is important that you save these keys in a safe place. You can
also delete these client credentials when the client applications no longer need
access to {C.CLOUD_LONG} resources. For more information about obtaining an access
token programmatically, see the
[{C.CLOUD_LONG} Terraform provider documentation](https://registry.terraform.io/providers/timescale/timescale/latest/docs).

<NumberedList>

<NumberedItem title={`Log in to ${C.CONSOLE} and click \`Settings\``}>

</NumberedItem>

<NumberedItem title="Click `Create credentials`">

</NumberedItem>

<NumberedItem title="In the `New client credentials` dialog, you can view the `Public key` and the `Secret key`">

Copy your secret key and store it in a secure place. You won't be able to
view the `Secret key` again in {C.CONSOLE}.

</NumberedItem>

<NumberedItem title="Click `Done`">

You can use these keys in your client applications to access {C.CLOUD_LONG}
resources inside the respective {C.PROJECT_SHORT}.
{C.CLOUD_LONG} generates a default `Name` for the client credentials.

</NumberedItem>

<NumberedItem title="Click the `⋮` menu and select `Rename credential`">

</NumberedItem>

<NumberedItem title="In the `Edit credential name` dialog, type the new name and click `Accept`">

</NumberedItem>

</NumberedList>

## Use client credentials to log in

Client credentials are also how you authenticate {C.CLI_LONG} and {C.MCP_LONG} non-interactively, for example in CI or from an AI agent that can't complete a browser login. For an interactive session on a machine without a browser, such as an SSH session or a container, you can instead run `tiger auth login --headless` and authorize with a device code from a browser on any machine; see [`tiger auth login`](/reference/tiger-cloud/tiger-cli#tiger-auth-login). To use client credentials, set them as environment variables, then log in:

```shell
TIGER_PUBLIC_KEY=<public_key> TIGER_SECRET_KEY=<secret_key> \
tiger auth login
```

The {C.PROJECT_SHORT} is auto-detected from your credentials. See [Authentication parameters](/reference/tiger-cloud/tiger-cli#authentication-parameters) for the equivalent `--public-key`/`--secret-key` flags.

## Delete client credentials

<NumberedList>

<NumberedItem title={`Log in to ${C.CONSOLE} and click \`Settings\``}>

</NumberedItem>

<NumberedItem title="Click the `⋮` menu of the client credential, and select `Delete`">

</NumberedItem>

<NumberedItem title="In the `Are you sure` dialog, type the name of the client credential, and click `Delete`">

</NumberedItem>

</NumberedList>

<TestPlan>

1. click `Settings`
2. expect url /settings
3. click `Create credentials`
4. expect label `New client credentials`
5. expect label `Secret key`
6. confirm `Done`
7. click `⋮` > `Rename credential`
8. type $CREDENTIAL_NAME into `Name`
9. confirm `Accept`
10. expect label $CREDENTIAL_NAME
11. use the row for $CREDENTIAL_NAME
12. click `⋮` > `Delete`
13. expect label `Are you sure`
14. type $CREDENTIAL_NAME into `Name`
15. confirm `Delete`
16. expect no label $CREDENTIAL_NAME

The page never names the credential it creates, so step 7 opens the first row's menu: the table lists
credentials newest first, and the one step 3 created is the newest. Every later row action is pinned
to the name step 8 gave it, so nothing else in the table is touched.

</TestPlan>
