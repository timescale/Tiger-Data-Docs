import * as C from "@constants";

If you have a large table, you can run `ANALYZE` on the target {C.SERVICE_LONG} to update the table statistics after the initial sync is complete. This helps the query planner make better decisions for query execution plans.

```bash
vacuumdb --analyze --verbose --jobs=8 --dbname="$TARGET"
```
