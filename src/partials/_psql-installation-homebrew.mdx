import { NumberedList, NumberedItem } from "@components/NumberedList";

#### Installing psql using Homebrew

<NumberedList>

<NumberedItem title="Install `psql`:">

```bash
brew install libpq
```

</NumberedItem>

<NumberedItem title="Update your path to include the `psql` tool.">

```bash
brew link --force libpq
```

On Intel chips, the symbolic link is added to `/usr/local/bin`. On Apple
Silicon, the symbolic link is added to `/opt/homebrew/bin`.

</NumberedItem>

</NumberedList>
