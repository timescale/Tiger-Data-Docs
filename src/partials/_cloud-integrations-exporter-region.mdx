import * as C from "../constants";
import { Callout } from "@stainless-api/docs/components";

<Callout variant="tip">

Your exporter must be in the same AWS region as the {C.SERVICE_LONG} it is attached to. 
If you have {C.SERVICE_LONG}s running in multiple regions, create an exporter for each region. 

</Callout>
