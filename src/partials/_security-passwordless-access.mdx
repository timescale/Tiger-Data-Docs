import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";
import TestPlan from "@components/TestPlan.astro";

Passwordless database access lets project members query a service through {C.SQL_EDITOR} and {C.DATA_MODE} without supplying database credentials. You control this access separately for each service.

Turning off passwordless database access disables {C.SQL_EDITOR} for the service and prevents {C.DATA_MODE} from connecting without database credentials. {C.DATA_MODE} remains available with database credentials and a network connection to the service. Project membership alone no longer grants database access through these tools.

If your security policy requires users to connect through your private network, turn off passwordless database access as well as configuring private networking. This setting controls the console's passwordless query connections; it does not change network restrictions or platform maintenance access.

To change the setting, you need permission to update the service, and the service must be running:

<NumberedList>
<NumberedItem title={`Select your service in ${C.CONSOLE}`}>

</NumberedItem>
<NumberedItem title="Open `Operations` > `Security` > `Passwordless access`">

</NumberedItem>
<NumberedItem title="Turn `Allow passwordless database access` on or off">

This setting was previously named `SQL Editor` and was under `Operations` > `Service management`.

</NumberedItem>
</NumberedList>

<TestPlan>

1. fork the service
2. select the service
3. click `Operations` > `Security` > `Passwordless access`
4. expect url /operations/security
5. toggle `Allow passwordless database access` off
6. expect no label `Click to expand`
7. toggle `Allow passwordless database access` on
8. go to /overview
9. expect label `Click to expand`

The page claims that turning the setting off disables the SQL editor. The editor is the collapsed
drawer at the foot of every service page, whose only text is `SQL Editor` and `Click to expand`, and
it is removed entirely while the setting is off. `SQL Editor` also appears in the setting's own
description, so the drawer's hint is the text that is only there while the editor is. Step 8 moves
to another service page before the last check so that no toast from the toggle is what gets searched.

</TestPlan>
