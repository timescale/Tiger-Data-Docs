import { Image } from "astro:assets";
import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";

import imgDatadog from "../assets/images/integrate/observability-alerting/tiger-cloud-integrations-datadog.png";

<NumberedList>

<NumberedItem title={`In ${C.CONSOLE}, open Exporters`}></NumberedItem>

<NumberedItem title="Click `New exporter`"></NumberedItem>

<NumberedItem title="Select `Metrics` for `Data type` and `Datadog` for provider">

<Image src={imgDatadog} alt="Adding a Datadog exporter in Tiger Console" />

</NumberedItem>

<NumberedItem title="Choose your AWS region and provide the API key">

The AWS region must be the same for your {C.CLOUD_LONG} exporter and the Datadog provider.

</NumberedItem>

<NumberedItem title="Set `Site` to your Datadog region"></NumberedItem>

<NumberedItem title="Export additional metrics">

Optionally tick `PostgreSQL metrics` to export [additional metrics](/integrate/observability-alerting/exported-metrics#postgresql-metrics), then click `Create exporter`.

</NumberedItem>

</NumberedList>
