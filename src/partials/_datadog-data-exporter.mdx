import * as C from "../constants";
import { Steps, Step } from "@stainless-api/docs/components";

<Steps>

<Step title={`In ${C.CONSOLE}, open Exporters`} />

<Step title="Click `New exporter`" />

<Step title="Select `Metrics` for `Data type` and `Datadog` for provider">

![Add Datadog exporter][add-datadog-exporter]

</Step>

<Step title="Choose your AWS region and provide the API key">

The AWS region must be the same for your {C.CLOUD_LONG} exporter and the Datadog provider.

</Step>

<Step title="Set `Site` to your Datadog region, then click `Create exporter`" />

</Steps>

[add-datadog-exporter]: ../assets/images/tiger-cloud-console-tiger-console-integrations-datadog.webp
[console-integrations]: https://console.cloud.timescale.com/dashboard/integrations
