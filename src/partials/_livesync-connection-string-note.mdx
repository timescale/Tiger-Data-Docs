import * as C from "@constants";
import { Callout } from "@stainless-api/docs/components";
import PasswordEncoder from "@components/PasswordEncoder";

<Callout variant="note">
When using the `postgres://user:pass@host:port/db` URI format, URL-encode special characters in the password to avoid URL-parsing errors. Alternatively, use the `keyword=value` parameter format instead of a URI; see the [libpq connection strings reference](https://www.postgresql.org/docs/current/libpq-connect.html#LIBPQ-CONNSTRING) (URI vs. parameter keywords).

<PasswordEncoder client:load />
</Callout>
