import * as C from "@constants";

To follow the procedure on this page you need to:

* Create a [target {C.SERVICE_LONG}][create-service].

   This procedure also works for [{C.SELF_LONG}][enable-timescaledb].

[create-service]: /getting-started/services/
[enable-timescaledb]: /self-hosted/install/
