import * as C from "@constants";

You can use hypershift to migrate existing {C.PG} databases in one step, and
enable compression and create hypertables instantly.

Use Hypershift to migrate your data to a {C.SERVICE_LONG} from these sources:

*   Standard {C.PG} databases
*   Amazon RDS databases
*   Other {C.COMPANY} databases, including {C.MST_LONG} and {C.SELF_LONG}

