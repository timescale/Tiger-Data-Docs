import * as C from "@constants";

import OpenSupportRequest from "./_migrate_open_support_request.mdx"

## 1. Set up a target database instance in {C.CLOUD_LONG}

[Create a {C.SERVICE_LONG}][create-service].

If you intend on migrating more than 400&nbsp;GB, open a support request to
ensure that enough disk is pre-provisioned on your {C.SERVICE_LONG}.

<OpenSupportRequest />

[create-service]: /getting-started/services/
