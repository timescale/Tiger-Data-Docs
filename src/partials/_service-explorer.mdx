import * as C from "@constants";

import ThemeImage from "@components/ThemeImage.astro";
import imgTigerConsoleServiceExplorerLight from "../assets/images/deploy/tiger-cloud/tiger-console-service-explorer.png";
import imgTigerConsoleServiceExplorerDark from "../assets/images/deploy/tiger-cloud/tiger-console-service-explorer-dark.png";
import imgTigerConsoleColumstoreDataSavingsLight from "../assets/images/deploy/tiger-cloud/tiger-console-columstore-data-savings.png";
import imgTigerConsoleColumstoreDataSavingsDark from "../assets/images/deploy/tiger-cloud/tiger-console-columstore-data-savings-dark.png";
import imgTigerConsoleServiceExplorerCaggsLight from "../assets/images/deploy/tiger-cloud/tiger-console-service-explorer-caggs.png";
import imgTigerConsoleServiceExplorerCaggsDark from "../assets/images/deploy/tiger-cloud/tiger-console-service-explorer-caggs-dark.png";

{C.SERVICE_SHORT_CAP} explorer in {C.CONSOLE} provides a rich administrative dashboard for
understanding the state of your database instance. The explorer gives you
insight into the performance of your database, giving you greater confidence and
control over your data.

The explorer works like an operations center as you develop and run your
applications with {C.CLOUD_LONG}. It gives you quick access to the key properties of
your database, like table sizes, schema definitions, and foreign key references,
as well as information specific to {C.CLOUD_LONG}, like information on your hypertables
and continuous aggregates.

To see the explorer, select your {C.SERVICE_SHORT} in {C.CONSOLE_SHORT} and click `Explorer`.

## General information

In the `General information` section, you can see a high-level
summary of your {C.SERVICE_SHORT}, including all your hypertables and
relational tables. It summarizes your overall compression ratios, and other
policy and continuous aggregate data. And, if you aren't already using key features like continuous aggregates, columnstore compression, or other automation policies and actions, it provides pointers to tutorials and documentation to help you get started.

Hover over `On-disk size` to split it into the {C.COLUMNSTORE} and {C.ROWSTORE} amounts. `Rowstore` here covers {C.HYPERTABLE} and {C.CAGG} data still in regular row format, including its indexes; plain {C.PG} tables are not counted.

<ThemeImage light={imgTigerConsoleServiceExplorerLight} dark={imgTigerConsoleServiceExplorerDark} alt="Service explorer" />

## Tables

You can have a detailed look into all your tables, including information about table schemas, table indexes, and
foreign keys. For your hypertables, it shows details about chunks, continuous
aggregates, and policies such as data retention policies and data reordering.
You can also inspect individual hypertables, including their sizes, dimension
ranges, and columnstore compression status.

From this section, you can also set an automated policy to compress chunks into the columnstore. For more information,
see the [hypercore documentation](/learn/columnar-storage/understand-hypercore).

When you inspect a {C.HYPERTABLE}, a timeline chart visualizes its {C.CHUNK}s, showing their relative sizes and whether each is in the {C.ROWSTORE} or the {C.COLUMNSTORE}. By default, the chart shows the latest 100 {C.CHUNK}s; click and drag to zoom in on a specific time range. The chart also shows the status of the latest runs for the {C.HYPERTABLE}'s {C.COLUMNSTORE} job, with a link to the job's deep dive page.

<ThemeImage light={imgTigerConsoleColumstoreDataSavingsLight} dark={imgTigerConsoleColumstoreDataSavingsDark} alt="Service explorer tables" />

For more information about hypertables, see the
[hypertables section](/learn/hypertables/understand-hypertables).

## {C.COLUMNSTORE_CAP}

In the `Columnstore` section, you see every {C.HYPERTABLE} and {C.CAGG} alongside how its data is stored right now:

- `Size`: the whole object on disk, indexes and TOAST included.
- `Rowstore`: the part of that size still in regular row format.
- `Columnstore`: the part converted to the compressed columnar format. Objects with no {C.COLUMNSTORE} data show `-`.
- `Compression`: how much space the conversion saves, in percent. Objects you have not converted yet show `Convert to columnstore` instead, which opens the conversion wizard.

Sort by `Size`, `Rowstore`, or `Columnstore` to find the objects that gain the most from the {C.COLUMNSTORE}.

## Continuous aggregates

In the `Continuous aggregate` section, you can see all your continuous
aggregates, including top-level information such as their size, whether they are
configured for real-time aggregation, and their refresh periods.

<ThemeImage light={imgTigerConsoleServiceExplorerCaggsLight} dark={imgTigerConsoleServiceExplorerCaggsDark} alt="Service explorer caggs" />

For more information about continuous aggregates, see the
[continuous aggregates section](/build/continuous-aggregates/create-a-continuous-aggregate).