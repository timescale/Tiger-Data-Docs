import * as C from "@constants";
import { Callout } from "@stainless-api/docs/components";

<Callout variant="note" title="Not the same as agent read-only mode">
This page covers read-only {C.PG} roles, which limit a database user's privileges. To restrict what an AI agent can do through {C.MCP_LONG}, see [best practices for AI agents](/build/tiger-cli-mcp/agent-best-practices).
</Callout>