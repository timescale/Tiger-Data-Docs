import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";

<NumberedList>
<NumberedItem title={`Ensure that the ${C.SERVICE_LONG} is running the ${C.PG} extensions used in your source database`}>

<ol type="a">
<li>

Check the extensions on the source database:

```bash
psql $SOURCE -c "SELECT * FROM pg_extension;"
```

</li>
<li>

For each extension, enable it on your target {C.SERVICE_LONG}:

```bash
psql $TARGET -c "CREATE EXTENSION IF NOT EXISTS <extension name> CASCADE;"
```

</li>
</ol>
</NumberedItem>
</NumberedList>
