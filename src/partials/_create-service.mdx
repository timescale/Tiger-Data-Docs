import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";

<NumberedList>

<NumberedItem title={`In the ${C.SERVICE_SHORT} creation page, click \`+ New service\`.`}>

Follow the wizard to configure your {C.SERVICE_SHORT} depending on its type.

</NumberedItem>

<NumberedItem title="Click `Create service`.">

Your {C.SERVICE_SHORT} is constructed and ready to use in a few seconds.

</NumberedItem>

<NumberedItem title={`Click \`Download the config\` and store the configuration information you need to connect to this ${C.SERVICE_SHORT} in a secure location.`}>

This file contains the passwords and configuration information you need to connect to your {C.SERVICE_SHORT} using the
{C.CONSOLE} {C.DATA_MODE}, from the command line, or using third-party database administration tools.

</NumberedItem>

</NumberedList>

You can [manage, pause, or delete][service-management] your {C.SERVICE_SHORT} at any time from {C.CONSOLE}.

[create-service]: https://console.cloud.timescale.com/dashboard/create_services
[service-management]: /use-timescale/services/service-management/
