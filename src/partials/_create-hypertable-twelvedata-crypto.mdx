import * as C from "@constants";
import { Steps, Step } from "@stainless-api/docs/components";

import HypertableIntro from "./_tutorials_hypertable_intro.mdx";
import CreateHypertablePolicyNote from "./_create-hypertable-columnstore-policy-note.mdx";

## Optimize time-series data in a hypertable

<HypertableIntro />

<Steps>

<Step title={`Connect to your ${C.SERVICE_LONG}`}>

In [{C.CONSOLE}][services-portal] open an [SQL editor][in-console-editors]. You can also connect to your service using [psql][psql].

</Step>

<Step title={`Create a ${C.HYPERTABLE} to store the real-time cryptocurrency data`}>

Create a [{C.HYPERTABLE}][hypertables-section] for your time-series data using [CREATE TABLE][hypertable-create-table].
For [efficient queries][secondary-indexes] on data in the columnstore, remember to `segmentby` the column you will
use most often to filter your data:

 ```sql
 CREATE TABLE crypto_ticks (
     "time" TIMESTAMPTZ,
     symbol TEXT,
     price DOUBLE PRECISION,
     day_volume NUMERIC
 ) WITH (
    tsdb.hypertable,
    tsdb.segmentby='symbol', 
    tsdb.orderby='time DESC'
 );
 ```
<CreateHypertablePolicyNote />

</Step>

</Steps>

## Create a standard {C.PG} table for relational data

When you have relational data that enhances your time-series data, store that data in
standard {C.PG} relational tables. 

<Steps>

<Step title="Add a table to store the asset symbol and name in a relational table">

```sql
CREATE TABLE crypto_assets (
    symbol TEXT UNIQUE,
    "name" TEXT
);
```

</Step>

</Steps>

You now have two tables within your {C.SERVICE_LONG}. A hypertable named `crypto_ticks`, and a normal
{C.PG} table named `crypto_assets`.

[hypertable-create-table]: /api/hypertable/create_table/
[hypertables-section]: /use-timescale/hypertables/
[in-console-editors]: /getting-started/run-queries-from-console/
[psql]: /integrations/psql
[secondary-indexes]: /use-timescale/hypercore/secondary-indexes/
[services-portal]: https://console.cloud.timescale.com/dashboard/services
