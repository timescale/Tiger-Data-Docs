---
title: Visualize data with Google Data Studio
description: Integrate Google Data Studio with Managed Service for TimescaleDB
products: [mst]
keywords: [integration, google data studio]
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 2
---

import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";

You can create reports or perform some analysis on data you have in {C.MST_LONG} using Google Data Studio. You can use Data Studio to
integrate other data sources, such as YouTube Analytics, MySQL, BigQuery,
AdWords, and others.

## Before you begin

*   You should also have a Google account.
*   In the overview page of your {C.MST_SERVICE_LONG}:
    *   Download the CA certificate named `ca.pem` for your {C.MST_SERVICE_SHORT}.
    *   Make a note of the `Host`, `Port`, `Database name`, `User`, and `Password`
        fields for the {C.MST_SERVICE_SHORT}.

## Connecting to a data source from Data Studio

<NumberedList>

<NumberedItem title="Create a new data source">

Log in to Google and open [Google Data Studio](https://lookerstudio.google.com/overview). Click the `Create +` button and choose `Data source`. Select `PostgreSQL` as the Google Connector.

</NumberedItem>

<NumberedItem title="Enter database details and enable SSL">

In the `Database Authentication` tab, type details for the `Host Name`,
`Port`, `Database`, `Username`, and `Password` fields. Select `Enable SSL` and upload your server certificate file, `ca.pem`.

</NumberedItem>

<NumberedItem title="Authenticate and connect">

Click `AUTHENTICATE`. Choose the table to be queried, or select `CUSTOM QUERY` to create an SQL query. Click `CONNECT`.

</NumberedItem>

</NumberedList>