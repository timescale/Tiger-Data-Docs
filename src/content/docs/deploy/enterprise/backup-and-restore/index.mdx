---
title: Back up and restore your cluster
description: Schedule, run, and restore backups for your TimescaleDB Enterprise cluster from the console
keywords: [timescaledb enterprise, backup manager, backups, point-in-time recovery, pitr, restore]
# products: [enterprise] — add once the "enterprise" product tag is added to content.config.ts
---

import * as C from "@constants";

TimescaleDB Enterprise runs incremental backups automatically from the console, and restores your
cluster to any point in time using those backups and write-ahead logs.

## Back up

- Scheduled backups run automatically on the schedule you set.
- Run an on-demand backup at any time from the console.
- To isolate backup storage from the database host, register a dedicated backup host when you
  [register your infrastructure](/get-started/enterprise/register-hosts).

## Restore to a point in time

Restore from the console by choosing a target time. The cluster restores to its state at that
point, using the incremental backups and write-ahead logs.