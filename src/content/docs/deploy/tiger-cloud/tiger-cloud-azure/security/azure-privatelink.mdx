---
title: Private endpoints
description: Connect your services to Azure Private Link endpoints to eliminate public internet exposure
products: [cloud]
---

import * as C from "@constants";
import AzurePrivateLink from "@partials/_azure-privatelink.mdx";

{C.CLOUD_LONG} supports private connectivity through Azure Private Link. Once configured, applications in your Azure Virtual Network reach your {C.SERVICE_SHORT} through a private endpoint on Microsoft's internal network, with no exposure to the public internet. This page walks you through provisioning the private endpoint in Azure, claiming it in {C.CONSOLE_LONG}, attaching it to your {C.SERVICE_SHORT}, and publishing the {C.SERVICE_SHORT} hostname in your own private DNS zone.

You control DNS resolution for your {C.SERVICE_SHORT} over Private Link. In a private DNS zone linked to your Virtual Network, you point the standard {C.SERVICE_SHORT} hostname at your private endpoint. Because you publish the same hostname that the public internet path uses, your connection strings, and the {C.SERVICE_SHORT} certificate, work unchanged.

<AzurePrivateLink />