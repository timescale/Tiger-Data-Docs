---
title: "Query another data source (FDW)"
description: "Query other Tiger Cloud services or external Postgres databases by using Postgres foreign data wrappers"
---

import * as C from "@constants";

You use {C.PG} foreign data wrappers (FDWs) to query external data sources from a Tiger Cloud service. These external data sources can be one of the following:

-   Other Tiger Cloud services
-   {C.PG} databases outside of Tiger Cloud

If you are using VPC peering, you can create FDWs in your Customer VPC to query a service in your Tiger Cloud project. However, you can’t create FDWs in your Tiger Cloud services to query a data source in your Customer VPC. This is because Tiger Cloud VPC peering uses AWS PrivateLink for increased security. See [VPC peering documentation](/deploy/tiger-cloud/secure-access/vpc-peering-and-aws-private-link) for additional details. {C.PG} FDWs are particularly useful if you manage multiple Tiger Cloud services with different capabilities, and need to seamlessly access and merge regular and time-series data.

Prerequisites

To follow the steps on this page:

-   Create a target [Tiger Cloud service](/deploy/tiger-cloud/get-started/create-services) with Real-time analytics enabled.
    
    You need [your connection details](/integrate/find-connection-details). This procedure also works for [self-hosted TimescaleDB](/get-started/choose-your-path/install-timescaledb).

Query another data source

To query another data source:

You create {C.PG} FDWs with the `postgres_fdw` extension, which is enabled by default in Tiger Cloud.

1.  **Connect to your service** See [how to connect](/deploy/tiger-cloud/get-started/run-queries-from-console).
2.  **Create a server** Run the following command using your [connection details](/integrate/find-connection-details):
3.  **Create user mapping** Run the following command using your [connection details](/integrate/find-connection-details):
4.  **Import a foreign schema (recommended) or create a foreign table**
    -   Import the whole schema:
    -   Alternatively, import a limited number of tables:
    -   Create a foreign table. Skip if you are importing a schema:

A user with the `tsdbadmin` role assigned already has the required `USAGE` permission to create {C.PG} FDWs. You can enable another user, without the `tsdbadmin` role assigned, to query foreign data. To do so, explicitly grant the permission. For example, for a new `grafana` user: