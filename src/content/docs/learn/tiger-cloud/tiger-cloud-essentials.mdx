---
title: Tiger Cloud essentials
description: Technical walkthrough covering hypertables, hypercore, continuous aggregates, and tiered storage in a single workflow
products: [cloud]
learnMore:
  relatedPostsHeading: Customer stories
  relatedPosts:
    - label: "How ApexAnalytica runs building telemetry, transactional data, and RAG on a single Postgres instance"
      href: https://www.tigerdata.com/blog/how-apexanalytica-runs-building-telemetry-transactional-data-rag-on-single-postgres-instance
    - label: "First commit to first customer: how startup Takton shipped in two months"
      href: https://www.tigerdata.com/blog/how-startup-takton-shipped-just-two-months-tiger-data
---

import * as C from "@constants";
import { Callout } from "@stainless-api/docs/components";
import { Tabs, TabItem } from "@astrojs/starlight/components";
import { NumberedList, NumberedItem } from "@components/NumberedList";

import IntegrationPrereqs from "../../../../partials/_prereqs-cloud-no-connection.mdx";
import { Prerequisites } from "@components/Prerequisites";
import HypercoreIntroShort from "../../../../partials/_hypercore-intro-short.mdx";
import HypercoreDirectCompress from "../../../../partials/_hypercore-direct-compress.mdx";

import CreateHypertablePolicyNote from "../../../../partials/_create-hypertable-columnstore-policy-note.mdx";
import HASetup from "../../../../partials/_high-availability-setup.mdx";
import TestPlan from "@components/TestPlan.astro";
import SupportPlans from "../../../../partials/_support-plans.mdx";

import ThemeImage from "@components/ThemeImage.astro";
import imgMutationLight from "../../../../assets/images/learn/deep-dive/hypercore-data-mutability-light.png";
import imgMutationDark from "../../../../assets/images/learn/deep-dive/hypercore-data-mutability-dark.png";
import imgHypertableStructureLight from "../../../../assets/images/learn/hypertable-structure.png";
import imgHypertableStructureDark from "../../../../assets/images/learn/hypertable-structure-dark.png";
import imgCaggsLight from "../../../../assets/images/learn/deep-dive/continuous-aggregates-light.png";
import imgCaggsDark from "../../../../assets/images/learn/deep-dive/continuous-aggregates-dark.png";
import imgCaggWizardLight from "../../../../assets/images/get-started/tools/continuous-aggregate-wizard-tiger-console.png";
import imgCaggWizardDark from "../../../../assets/images/get-started/tools/continuous-aggregate-wizard-tiger-console-dark.png";
import imgTieredStorageLight from "../../../../assets/images/learn/deep-dive/tiered-storage-light.png";
import imgTieredStorageDark from "../../../../assets/images/learn/deep-dive/tiered-storage-dark.png";
import imgEnableTieredStorageLight from "../../../../assets/images/build/data-management/enable-tiered-storage-tiger-console.png";
import imgEnableTieredStorageDark from "../../../../assets/images/build/data-management/enable-tiered-storage-tiger-console-dark.png";

{C.CLOUD_LONG} offers managed database services that provide a stable and reliable environment for your
applications.

Each {C.SERVICE_LONG} is a single optimised {C.PG} instance extended with innovations such as {C.TIMESCALE_DB} in the database
engine, in a cloud infrastructure that delivers speed without sacrifice. A radically faster {C.PG} for transactional,
analytical, and agentic workloads at scale.

{C.CLOUD_LONG} scales {C.PG} to ingest and query vast amounts of live data. {C.CLOUD_LONG}
provides a range of features and optimizations that supercharge your queries while keeping the
costs down. For example:

- The {C.HYPERCORE} row-columnar engine in {C.TIMESCALE_DB} makes queries up to 350x faster, ingests 44% faster, and reduces
  storage by 90%.
- Tiered storage in {C.CLOUD_LONG} seamlessly moves your data from high performance storage for frequently accessed data to
  low cost bottomless storage for rarely accessed data.

The following figure shows how {C.TIMESCALE_DB} optimizes your data for superfast real-time analytics:

<ThemeImage light={imgMutationLight} dark={imgMutationDark} alt="Data mutability in hypercore: inserts, updates, and deletes are routed to an interim rowstore chunk that periodically recompresses into the columnstore, supporting full mutability without rewriting compressed data" />

This page shows you how to rapidly implement the features in {C.CLOUD_LONG} that enable you to
ingest and query data faster while keeping the costs low.

<Prerequisites context="page">
<IntegrationPrereqs />
</Prerequisites>

## Optimize time-series data in {C.HYPERTABLE}s with {C.HYPERCORE}

Time-series data represents the way a system, process, or behavior changes over time. {C.HYPERTABLE_CAP}s are {C.PG} tables
that help you improve insert and query performance by automatically partitioning your data by time. Each {C.HYPERTABLE}
is made up of child tables called {C.CHUNK}s. Each {C.CHUNK} is assigned a range of time, and only
contains data from that range. When you run a query, {C.TIMESCALE_DB} identifies the correct {C.CHUNK} and runs the query on
it, instead of going through the entire table. You can also tune {C.HYPERTABLE}s to increase performance even more.

<ThemeImage light={imgHypertableStructureLight} dark={imgHypertableStructureDark} alt="Hypertable structure" />

<HypercoreIntroShort />

{C.HYPERTABLE_CAP}s exist alongside regular {C.PG} tables.
You use regular {C.PG} tables for relational data, and interact with {C.HYPERTABLE}s
and regular {C.PG} tables in the same way.

This section shows you how to create regular tables and {C.HYPERTABLE}s, and import
relational and time-series data from external files.

<NumberedList>

  <NumberedItem title={`Import some time-series data into ${C.HYPERTABLE}s`}>

    1. Unzip [crypto_sample.zip](https://assets.timescale.com/docs/downloads/candlestick/crypto_sample.zip) to a `<local folder>`.

       This test dataset contains:
         - Second-by-second data for the most-traded crypto-assets. This time-series data is best suited for
           optimization in a [{C.HYPERTABLE}](/learn/hypertables/understand-hypertables).
         - A list of asset symbols and company names. This is best suited for a regular relational table.

       To import up to 100 GB of data directly from your current {C.PG}-based database,
       [migrate with downtime](/migrate/migrate-with-downtime) using native {C.PG} tooling. To seamlessly import 100GB-10TB+
       of data, use [Livesync replication](/migrate/livesync-replication). To add data from non-{C.PG} data
       sources, see [Import and ingest data](/migrate).

    2. Upload data into a {C.HYPERTABLE}:

       To more fully understand how to create a {C.HYPERTABLE}, how {C.HYPERTABLE}s work, and how to optimize them for
       performance by tuning {C.CHUNK} intervals and enabling {C.CHUNK_SKIPPING}, see
       [the {C.HYPERTABLE}s documentation](/learn/hypertables/understand-hypertables).

       <Tabs>

       <TabItem label={C.CONSOLE}>

          {C.CONSOLE} data upload creates {C.HYPERTABLE}s and relational tables from the data you are uploading:
          1. In [{C.CONSOLE}](https://console.cloud.tigerdata.com/dashboard/services), select the {C.SERVICE_SHORT} to add data to, then click `Actions` > `Import data`.
          1. In `Upload Files`, click `Upload your files`, then click `CSV`.
          1. In `Upload .CSV`, click to browse, or drag and drop `<local folder>/tutorial_sample_tick.csv` to upload.
          1. Leave the default settings for the delimiter, skipping the header, and creating a new table.
          1. In `Table`, provide `crypto_ticks` as the new table name.
          1. Enable `Hypertable partition` for the `time` column and click `Continue`.

              The upload wizard creates a {C.HYPERTABLE} containing the data from the CSV file.
          1. When the data is uploaded, press `Run` to have a quick look at your data, then close `Upload .CSV`.
          1. Repeat the process with `<local folder>/tutorial_sample_assets.csv` and rename to `crypto_assets`.

              There is no time-series data in this table, so you don't see the `Hypertable partition` option.

       </TabItem>

       <TabItem label="psql">

       1. In the terminal, navigate to `<local folder>` and connect to your {C.SERVICE_SHORT}.
          ```bash
          psql -d "postgres://<username>:<password>@<host>:<port>/<database-name>"
          ```
          You use your [connection details](/integrate/find-connection-details) to fill in this {C.PG} connection string.

       2. Create tables for the data to import:

          - For the time-series data:

                In your sql client, create a {C.HYPERTABLE}:

                Create a [{C.HYPERTABLE}](/learn/hypertables/understand-hypertables) for your time-series data using CREATE TABLE.
                For [efficient queries](/build/performance-optimization/secondary-indexes), remember to `segmentby` the column you will
                use most often to filter your data. For example:

                ```sql
                CREATE TABLE crypto_ticks (
                  "time" TIMESTAMPTZ,
                  symbol TEXT,
                  price DOUBLE PRECISION,
                  day_volume NUMERIC
                ) WITH (
                   tsdb.hypertable,
                   tsdb.segmentby = 'symbol'
                );
                ```
                <CreateHypertablePolicyNote />

          - For the relational data:

             In your sql client, create a normal {C.PG} table:
             ```sql
             CREATE TABLE crypto_assets (
              symbol TEXT NOT NULL,
              name TEXT NOT NULL
             );
            ```
       3. Speed up data ingestion:

          <HypercoreDirectCompress />

       4. Upload the dataset to your {C.SERVICE_SHORT}:

          ```sql
          \COPY crypto_ticks from './tutorial_sample_tick.csv' DELIMITER ',' CSV HEADER;
          ```

          ```sql
          \COPY crypto_assets from './tutorial_sample_assets.csv' DELIMITER ',' CSV HEADER;
          ```

       </TabItem>

       </Tabs>

  </NumberedItem>

  <NumberedItem title="Have a quick look at your data">

    You query {C.HYPERTABLE}s in exactly the same way as you would a relational {C.PG} table.
    Use one of the following SQL editors to run a query and see the data you uploaded:

    - **{C.DATA_MODE}**: write queries, visualize data, and share your results in [{C.CONSOLE}](https://console.cloud.tigerdata.com/dashboard/services?popsql) for all your {C.SERVICE_LONG}s.
    - **{C.SQL_EDITOR}**: write, fix, and organize SQL faster and more accurately in [{C.CONSOLE}](https://console.cloud.tigerdata.com/dashboard/services) for a {C.SERVICE_LONG}.
    - **psql**: easily run queries on your {C.SERVICE_LONG}s or {C.SELF_LONG} deployment from the terminal.

    ```sql
    SELECT * FROM crypto_ticks
    ORDER BY time DESC
    LIMIT 10;
    ```

  </NumberedItem>
</NumberedList>

## Write fast and efficient analytical queries

Aggregation is a way of combing data to get insights from it. Average, sum, and count are all
examples of simple aggregates. However, with large amounts of data, aggregation slows things down, quickly.
{C.CAGG_CAP}s are a kind of {C.HYPERTABLE} that is refreshed automatically in
the background as new data is added, or old data is modified. Changes to your dataset are tracked,
and the {C.HYPERTABLE} behind the {C.CAGG} is automatically updated in the background.

<ThemeImage light={imgCaggsLight} dark={imgCaggsDark} alt="How a continuous aggregate incrementally materializes time-bucketed rollups from a raw hypertable, with a real-time aggregate query reading both pre-computed and recent raw data" />

You create {C.CAGG}s on uncompressed data in high-performance storage. They continue to work
on [data in the {C.COLUMNSTORE}](#write-fast-and-efficient-analytical-queries)
and [rarely accessed data in tiered storage](#slash-storage-charges). You can even
create [{C.CAGG}s on top of your {C.CAGG}s](/learn/continuous-aggregates/hierarchical-continuous-aggregates).

You use {C.TIME_BUCKET}s to create a {C.CAGG}. {C.TIME_BUCKET_CAP}s aggregate data in {C.HYPERTABLE}s by time
interval. For example, a 5-minute, 1-hour, or 3-day bucket. The data grouped in a {C.TIME_BUCKET} uses a single
timestamp. {C.CAGG_CAP}s minimize the number of records that you need to look up to perform your
query.

This section shows you how to run fast analytical queries using {C.TIME_BUCKET}s and {C.CAGG}s in
{C.CONSOLE}. You can also do this using psql.

<Tabs>

<TabItem label={C.DATA_MODE}>

<NumberedList>
  <NumberedItem title={`In ${C.CONSOLE}, toggle ${C.DATA_MODE} at the top right`}></NumberedItem>

<NumberedItem title={`Connect to your ${C.SERVICE_SHORT}`}>
  In the connection drop-down in the top right, select your {C.SERVICE_SHORT}.
</NumberedItem>

  <NumberedItem title={`Create a ${C.CAGG}`}>

    For a {C.CAGG}, data grouped using a {C.TIME_BUCKET} is stored in a
    {C.PG} `MATERIALIZED VIEW` in a {C.HYPERTABLE}. `timescaledb.continuous` ensures that this data
    is always up to date.
    In {C.DATA_MODE}, use the following code to create a {C.CAGG} on the real-time data in
    the `crypto_ticks` table:

    ```sql
    CREATE MATERIALIZED VIEW assets_candlestick_daily
    WITH (timescaledb.continuous) AS
    SELECT
      time_bucket('1 day', "time") AS day,
      symbol,
      max(price) AS high,
      first(price, time) AS open,
      last(price, time) AS close,
      min(price) AS low
    FROM crypto_ticks srt
    GROUP BY day, symbol;
    ```

    This {C.CAGG} creates the [candlestick chart](https://www.investopedia.com/terms/c/candlestick.asp) data you use to visualize
    the price change of an asset.

  </NumberedItem>

  <NumberedItem title="Create a policy to refresh the view every hour">
   ```sql
   SELECT add_continuous_aggregate_policy('assets_candlestick_daily',
   start_offset => INTERVAL '3 weeks',
   end_offset => INTERVAL '24 hours',
   schedule_interval => INTERVAL '3 hours');
   ```
  </NumberedItem>

  <NumberedItem title="Have a quick look at your data">

    You query {C.CAGG}s exactly the same way as your other tables. To query the `assets_candlestick_daily`
    {C.CAGG} for all assets:

    ```sql
    SELECT * FROM assets_candlestick_daily
    ORDER BY day DESC, symbol
    LIMIT 10;
    ```

  </NumberedItem>
</NumberedList>

</TabItem>

<TabItem label={`${C.CAGG_CAP} wizard`}>

<NumberedList>

<NumberedItem title={`In ${C.CONSOLE}, select the ${C.SERVICE_SHORT} you uploaded data to`}></NumberedItem>

<NumberedItem title="Click `Explorer` > `Continuous Aggregates`, open the `Hypertables` tab, then click `Create a Continuous Aggregate` next to `crypto_ticks`">

The `Hypertables` tab lists every {C.HYPERTABLE} in the {C.SERVICE_SHORT} with the number of
{C.CAGG}s on it. The control only appears once the {C.SERVICE_SHORT} has a {C.HYPERTABLE}.

</NumberedItem>

<NumberedItem title="In `Configure the aggregate`, type `assets_candlestick_daily` in `View name`, then click `Continue`">

<ThemeImage light={imgCaggWizardLight} dark={imgCaggWizardDark} alt="Continuous aggregate wizard" />

`Hypertable` and `Time column` are already set to `crypto_ticks` and `time`.

</NumberedItem>

<NumberedItem title="In `Review the view`, select a `Bucket interval` of `1 day`">

`Bucket interval` sets how wide each aggregation bucket is. The wizard writes the matching
`CREATE MATERIALIZED VIEW` statement below it, which you can edit before running.

</NumberedItem>

<NumberedItem title="Replace the statement with the following, then click `Run and proceed`">

````sql
CREATE MATERIALIZED VIEW assets_candlestick_daily
WITH (timescaledb.continuous) AS
SELECT
  time_bucket('1 day', "time") AS bucket,
  symbol,
  max(price) AS high,
  first(price, time) AS open,
  last(price, time) AS close,
  min(price) AS low
FROM "public"."crypto_ticks" srt
GROUP BY bucket, symbol;
````

The view is created empty. The refresh policy you set up next is what populates it.

</NumberedItem>

<NumberedItem title="In `Configure the refresh policy`, set the following values, then click `Continue`">

- `Start offset`: `3 weeks`, how far back to materialize
- `End offset`: `24 hours`, how recent to exclude
- `Schedule interval`: `3 hours`, how often the refresh job runs

</NumberedItem>

<NumberedItem title="In `Review the refresh policy`, click `Run and proceed`">

The wizard shows the `add_continuous_aggregate_policy` call it is about to run. Creating the policy
triggers the first refresh, back as far as the `Start offset`.

</NumberedItem>

<NumberedItem title="Click `Close`"></NumberedItem>

</NumberedList>

</TabItem>

</Tabs>

To see the change in terms of query time and data returned between a regular query and
a {C.CAGG}, run the query part of the {C.CAGG}
( `SELECT ...GROUP BY day, symbol;` ) and compare the results.

## Slash storage charges

In the previous sections, you used {C.CAGG}s to make fast analytical queries, and
{C.HYPERCORE} to reduce storage costs on frequently accessed data. To reduce storage costs even more,
you create tiering policies to move rarely accessed data to the object store. The object store is
low-cost bottomless data storage built on Amazon S3 or Azure Blob storage. However, no matter the tier, you can
[query your data when you need](/build/data-management/storage/query-tiered-data). {C.CLOUD_LONG} seamlessly accesses the correct storage
tier and generates the response.

<ThemeImage light={imgTieredStorageLight} dark={imgTieredStorageDark} alt="Tiered storage in Tiger Cloud: a hypertable's rowstore and columnstore chunks live on high-performance block storage, while older chunks tier to low-cost bottomless object storage and remain transparently queryable" />

To set up data tiering:

<NumberedList>

<NumberedItem title="Enable data tiering">

 1. In [{C.CONSOLE}](https://console.cloud.tigerdata.com/dashboard/services), select the {C.SERVICE_SHORT} to modify.

 1. In `Explorer`, click `Data Tiering` > `Enable tiered storage`.

     <ThemeImage light={imgEnableTieredStorageLight} dark={imgEnableTieredStorageDark} alt="Enable tiered storage" />

     When tiered storage is enabled, you see the amount of data in the tiered object storage.
</NumberedItem>
<NumberedItem title="Set the time interval when data is tiered">

 In {C.CONSOLE}, toggle {C.DATA_MODE}, then set up a tiering policy on a {C.HYPERTABLE} with the following query:
  ```sql
  SELECT add_tiering_policy('assets_candlestick_daily', INTERVAL '3 weeks');
  ```

</NumberedItem>

<NumberedItem title="Query tiered data">
 You enable reads from tiered data for each query, for a session or for all future
 sessions. To run a single query on tiered data:

 1. Enable reads on tiered data:
    ```sql
    set timescaledb.enable_tiered_reads = true
    ```
 1. Query the data:
    ```sql
    SELECT * FROM crypto_ticks srt LIMIT 10
    ```
 1. Disable reads on tiered data:
    ```sql
    set timescaledb.enable_tiered_reads = false;
    ```
 For more information, see [Querying tiered data](/build/data-management/storage/query-tiered-data).

</NumberedItem>
</NumberedList>

## Reduce the risk of downtime and data loss

By default, all {C.SERVICE_LONG}s have rapid recovery enabled. However, if your app has very low tolerance
for downtime, {C.CLOUD_LONG} offers {C.HA_REPLICA}s. {C.HA_REPLICA_SHORT}s are exact, up-to-date copies
of your database hosted in multiple AWS availability zones (AZ) within the same region as your primary node.
{C.HA_REPLICA_SHORT}s automatically take over operations if the original primary data node becomes unavailable.
The primary node streams its write-ahead log (WAL) to the replicas to minimize the chances of
data loss during failover.

<HASetup />

For more information, see [{C.HA_REPLICA_CAP}](/deploy/tiger-cloud/tiger-cloud-aws/high-availability/high-availability).

## {C.CLOUD_LONG} support

<SupportPlans />

What next? See [Build](/build) for more advanced tutorials, interact with the data in your {C.SERVICE_LONG} using
[your favorite programming language](/integrate/code/connect-your-app), integrate your {C.SERVICE_LONG} with a range of
[third-party tools](/integrate), [build with {C.COMPANY} products](/build), or dive into [the API reference](/reference).


<TestPlan>

**Isolation**: fork. The run creates tables and a {C.CAGG}, turns on tiered storage, and changes the
high-availability configuration, all of which are service-level changes.

**Setup**: fetch https://assets.timescale.com/docs/downloads/candlestick/crypto_sample.zip — the
dataset the page tells the reader to unzip. The files are removed when the run ends.

**Steps**:
1. select the service
2. run command: psql -d "postgres://<username>:<password>@<host>:<port>/<database-name>"
3. run SQL:
     CREATE TABLE crypto_ticks ("time" TIMESTAMPTZ, symbol TEXT, price DOUBLE PRECISION,
     day_volume NUMERIC) WITH (tsdb.hypertable, tsdb.segmentby = 'symbol');
4. run SQL:
     CREATE TABLE crypto_assets (symbol TEXT NOT NULL, name TEXT NOT NULL);
5. run SQL: SET timescaledb.enable_direct_compress_insert = on;
6. run SQL: \COPY crypto_ticks from './tutorial_sample_tick.csv' DELIMITER ',' CSV HEADER;
7. run SQL: \COPY crypto_assets from './tutorial_sample_assets.csv' DELIMITER ',' CSV HEADER;
8. run SQL: SELECT * FROM crypto_ticks ORDER BY time DESC LIMIT 10;
9. expect rows: SELECT 1 FROM crypto_ticks LIMIT 1
10. expect rows: SELECT 1 FROM crypto_assets LIMIT 1
11. expect rows: SELECT 1 FROM timescaledb_information.hypertables WHERE hypertable_name = 'crypto_ticks'
12. run SQL: DROP TABLE crypto_ticks;
13. run SQL: DROP TABLE crypto_assets;
14. exit psql
15. click `Actions` > `Import data`
16. click `Upload your files`, then click `CSV`
17. upload tutorial_sample_tick.csv
18. type `crypto_ticks` into `Table`
19. toggle `Hypertable partition` on for `time`
20. click `Continue`
21. click `Run`
22. click `Close`
23. click `Actions` > `Import data`
24. click `Upload your files`, then click `CSV`
25. upload tutorial_sample_assets.csv
26. type `crypto_assets` into `Table`
27. click `Continue`
28. click `Run`
29. click `Close`
30. expect rows: SELECT 1 FROM crypto_ticks LIMIT 1
31. expect rows: SELECT 1 FROM crypto_assets LIMIT 1
32. expect rows: SELECT 1 FROM timescaledb_information.hypertables WHERE hypertable_name = 'crypto_ticks'
33. run SQL:
     CREATE MATERIALIZED VIEW assets_candlestick_daily WITH (timescaledb.continuous) AS
     SELECT time_bucket('1 day', "time") AS day, symbol, max(price) AS high,
     first(price, time) AS open, last(price, time) AS close, min(price) AS low
     FROM crypto_ticks srt GROUP BY day, symbol;
34. run SQL:
     SELECT add_continuous_aggregate_policy('assets_candlestick_daily',
     start_offset => INTERVAL '3 weeks', end_offset => INTERVAL '24 hours',
     schedule_interval => INTERVAL '3 hours');
35. run SQL: SELECT * FROM assets_candlestick_daily ORDER BY day DESC, symbol LIMIT 10;
36. expect rows: SELECT 1 FROM timescaledb_information.continuous_aggregates WHERE view_name = 'assets_candlestick_daily'
37. click `Explorer` > `Data Tiering`
38. click `Enable tiered storage`
39. click `Let's go!`
40. run SQL: SELECT add_tiering_policy('assets_candlestick_daily', INTERVAL '3 weeks');
41. run SQL: set timescaledb.enable_tiered_reads = true;
42. run SQL: SELECT * FROM crypto_ticks srt LIMIT 10;
43. run SQL: set timescaledb.enable_tiered_reads = false;
44. go to /overview
45. click `Operations` > `High availability`
46. expect url /operations
47. choose an unselected option
48. click `Change configuration`
49. click `Change config`

**Notes**: both documented routes into the data are walked, on the real files and the real table
names. Steps 2 to 14 take the `psql` route and then drop what it created: the two routes are
alternatives that build the same tables, so the first has to be undone before the second can be
tested as written. Steps 15 to 29 take the {C.CONSOLE} route, including the `Hypertable partition`
toggle, which is the point of that route and only appears for a time column; the data it leaves
behind is what the sections after it use.

Assertions are steps, not a separate section. `run SQL:` only fails when a statement errors, so a
`SELECT` over an empty table passes — without steps 9 to 11 and 30 to 32 this plan could walk an
entire ingest route, load nothing, and report a pass.

Deliberately unscripted:

- `ALTER TABLE metrics SET (timescaledb.direct_compress)`. It comes from an explanatory partial
  embedded in the ingestion step and names a table this page never creates, so running it proves
  nothing. Worth fixing in the page rather than working around here.
- The {C.DATA_MODE} route. It is PopSQL in a cross-origin iframe, which the walker cannot drive. Its
  SQL is exercised through the steps above, so only the route goes untested.
- The {C.CAGG_CAP} wizard route. The Console wizard was redesigned and the page still describes the
  old one, so a script written now would encode instructions already known to be wrong. It gets steps
  once that section is rewritten.

</TestPlan>
