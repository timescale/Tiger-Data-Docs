---
title: Understand hypertables
description: Hypertables are PostgreSQL tables with special features that power real-time analytics on time-series and event data
products: [cloud, mst, self_hosted]
---

import * as C from "@constants";
import { Callout } from "@stainless-api/docs/components";
import { NumberedList, NumberedItem } from "@components/NumberedList";
import HypertableOverview from "../../../../partials/_hypertable-intro.mdx";
import ChunkInterval from "../../../../partials/_chunk-interval.mdx";

<HypertableOverview />

## Partition by time

Each {C.HYPERTABLE} is partitioned into child {C.HYPERTABLE}s called {C.CHUNK}s. Each {C.CHUNK} is assigned
a range of time, and only contains data from that range.

### Time partitioning

Typically, you partition {C.HYPERTABLE}s on columns that hold time values.
[Best practice is to use `timestamptz`](https://wiki.postgresql.org/wiki/Don't_Do_This#Don.27t_use_timestamp_.28without_time_zone.29) column type. However, you can also partition on
`date`, `integer`, `timestamp` and [UUIDv7](/reference/timescaledb/uuid-functions) types.

By default, each {C.HYPERTABLE} {C.CHUNK} holds data for 7 days. You can change this to better suit your
needs. For example, if you set `chunk_interval` to 1 day, each {C.CHUNK} stores data for a single day.

{C.TIMESCALE_DB} divides time into potential {C.CHUNK} ranges, based on the `chunk_interval`. Each {C.HYPERTABLE} {C.CHUNK} holds
data for a specific time range only. When you insert data from a time range that doesn't yet have a {C.CHUNK}, {C.TIMESCALE_DB}
automatically creates a {C.CHUNK} to store it.

In practice, this means that the start time of your earliest {C.CHUNK} does not
necessarily equal the earliest timestamp in your {C.HYPERTABLE}. Instead, there
might be a time gap between the start time and the earliest timestamp. This
doesn't affect your usual interactions with your {C.HYPERTABLE}, but might affect
the number of {C.CHUNK}s you see when inspecting it.

## Best practices for scaling and partitioning

Best practices for maintaining a high performance when scaling include:

- Limit the number of {C.HYPERTABLE}s in your {C.SERVICE_SHORT}; having tens of thousands of {C.HYPERTABLE}s is not recommended.
- Choose a strategic {C.CHUNK} size.

{C.CHUNK_CAP} size affects insert and query performance. You want a {C.CHUNK} small enough
to fit into memory so you can insert and query recent data without
reading from disk. However, having too many small and sparsely filled {C.CHUNK}s can
affect query planning time and compression. The more {C.CHUNK}s in the system, the slower that process becomes, even more so
when all those {C.CHUNK}s are part of a single {C.HYPERTABLE}.

<ChunkInterval />

For a detailed analysis of how to optimize your {C.CHUNK} sizes, see the
[blog post on chunk time intervals](https://www.tigerdata.com/blog/timescale-cloud-tips-testing-your-chunk-size). To learn how
to view and set your {C.CHUNK} time intervals, see
[Optimize {C.HYPERTABLE} {C.CHUNK} intervals](/build/performance-optimization/improve-hypertable-performance#optimize-hypertable-chunk-intervals).

## {C.HYPERTABLE_CAP} indexes

By default, indexes are automatically created when you create a {C.HYPERTABLE}. The default index is on time, descending.
You can prevent index creation by setting the `create_default_indexes` option to `false`.

{C.HYPERTABLE_CAP}s have some restrictions on unique constraints and indexes. If you
want a unique index on a {C.HYPERTABLE}, it must include all the partitioning
columns for the table. To learn more, see
[Enforce constraints with unique indexes on {C.HYPERTABLE}s](/build/performance-optimization/hypertables-and-unique-indexes).

## Partition by dimension

Partitioning on time is the most common use case for {C.HYPERTABLE}s, but it may not be enough for your needs. For example,
you may need to scan for the latest readings that match a certain condition without locking a critical {C.HYPERTABLE}.

<Callout variant="note">
The use case for a partitioning dimension is a multi-tenant setup. You isolate the tenants using the `tenant_id` space
partition. However, you must perform extensive testing to ensure this works as expected, and there is a strong risk of
partition explosion.
</Callout>

You add a partitioning dimension at the same time as you create the {C.HYPERTABLE}, when the table is empty. The good news
is that although you select the number of partitions at creation time, as your data grows you can change the number of
partitions later and improve query performance. Changing the number of partitions only affects {C.CHUNK}s created after the
change, not existing {C.CHUNK}s. To set the number of partitions for a partitioning dimension, call `set_number_partitions`.
For example:

<NumberedList>
<NumberedItem title={`Create the ${C.HYPERTABLE} with a 1-day chunk interval`}>
```sql
CREATE TABLE conditions(
   "time"      timestamptz not null,
   device_id   integer,
   temperature float
)
WITH(
   timescaledb.hypertable,
   timescaledb.chunk_interval='1 day'
);
```
</NumberedItem>

<NumberedItem title="Add a hash partition on a non-time column">
```sql
select * from add_dimension('conditions', by_hash('device_id', 3));
```
Now use your {C.HYPERTABLE} as usual, but you can also ingest and query efficiently by the `device_id` column.
</NumberedItem>

<NumberedItem title="Change the number of partitions as your data grows">
```sql
select set_number_partitions('conditions', 5, 'device_id');
```
</NumberedItem>
</NumberedList>
