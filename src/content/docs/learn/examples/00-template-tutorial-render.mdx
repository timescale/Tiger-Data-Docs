---
title: Tutorial title in sentence case
description: One-sentence description in sentence case for search and cards.
products: [cloud, self_hosted]
keywords: [tutorials, topic, level, time-series, ...]
---

{/* markdownlint-disable MD033 */}
{/*
  This is an MDX template. When you add a new tutorial to the docs, copy this file
  into the appropriate content directory (e.g. src/content/docs/learn/examples/) and
  save it with a .mdx extension so components render correctly.
*/}

import { Callout } from "@stainless-api/docs/components";
import { NumberedList, NumberedItem } from "@components/NumberedList";
import { LinkCard, CardGrid } from "@astrojs/starlight/components";

{/* Optional: use shared constants for product names (Tiger Cloud, TimescaleDB, etc.) */}
{/* import * as C from "@constants"; */}

<Callout variant="tip">
  **Using this template:** Replace all placeholders in (brackets), remove or
  customize the template guide callouts, and add your own content. For more on
  tutorial structure, see the [Good Docs Project tutorial
  guide](https://www.thegooddocsproject.dev/).
</Callout>

(Optional) Add a short intro or video link here. You can also watch a walkthrough [on the Tiger Data YouTube channel](https://www.youtube.com/@tigerdata) (insert video link when available).

## Overview

In this tutorial, you'll learn how to (brief description of the main task). This tutorial is for (audience). It assumes you have basic knowledge of:

- Concept 1
- Concept 2
- Concept 3

By the end, you'll be able to:

- Learning objective 1
- Learning objective 2
- Learning objective 3

## Background

(Optional. Use this section when the product or use case needs a short setup.)

- (Product) is a (type) you can use to (common use case).
- (Product) provides many of the same features as (alternatives), and with (feature) you can (benefit).
- Using (feature) enables you to (solve pain point).

## Prerequisites

Before you start, make sure you have:

- Prerequisite 1 (e.g. a Tiger Cloud account or self-hosted setup)
- Prerequisite 2 (e.g. a SQL client: psql, pgAdmin, or similar)
- Prerequisite 3 (e.g. expected prior knowledge or tools)

<Callout variant="note">
  Mention any optional prerequisites or environment details (e.g. Docker,
  specific extensions) here if needed.
</Callout>

## (Task name)

(Optional: one sentence on the purpose of this task if the title doesn’t make it clear.)

<NumberedList>
  <NumberedItem title="(Step 1 — use a verb to start)">

(Optional) Short explanation.

(Optional) Code sample or screenshot.

(Optional) What the user should see or verify after this step.

  </NumberedItem>

  <NumberedItem title="(Step 2 — use a verb to start)">

(Optional) Explanation, code, or result.

  </NumberedItem>

  <NumberedItem title="(Step 3 — use a verb to start)">

(Optional) Explanation, code, or result.

  </NumberedItem>
</NumberedList>

## Summary

In this tutorial, you learned how to:

- Summary point 1
- Summary point 2
- Summary point 3

## Next steps

Point readers to related tutorials or docs using the card grid below. Group cards by theme (e.g. fundamentals, build, examples).

### Learn more

{/* add related docs pages and topics here */}

<CardGrid>
  <LinkCard
    title="(Doc title)"
    description="(Short description for the card)"
    href="(/path/to/doc)"
  />
  <LinkCard
    title="(Doc title)"
    description="(Short description)"
    href="(/path/to/doc)"
  />
</CardGrid>

### Other examples

{/* add other examples and tutorials here */}

<CardGrid>
  <LinkCard
    title="(Example or tutorial title)"
    description="(Short description)"
    href="(/path/to/example)"
  />
</CardGrid>
