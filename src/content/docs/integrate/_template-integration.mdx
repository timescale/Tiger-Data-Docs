---
title: Integrate (third-party product name) with (Tiger Data product name)
description: One-sentence SEO optimized description
integrationCategory: (Integration category)
integrationPlatforms: [(platform 1), (platform 2)]
integrationIndustry: [(industry 1), (industry 2), (industry 3)]
keywords: [(topic 1), (topic 2), (topic 3), (topic 4), (topic 5)]
integrationCardLogo: product-name.png
sidebar:
  label: (Product name)
---

{/* Template for a new integration page. To use it:
- Copy to src/content/docs/integrate/(category)/(slug).mdx
- Fill in the (placeholders)
- Keep as many parts as needed - some integration guides just have one, others have four.
- Remove unused optional elements/sections.

Works best when paired with the write-docs-integration-guide skill at .claude/skills/write-docs-integration-guide/SKILL.md.*/}

{/* Required imports - always have those:*/}
import * as C from "@constants";
import { Prerequisites } from "@components/Prerequisites";
import { NumberedList, NumberedItem } from "@components/NumberedList";
import IntegrationPrereqs from "@partials/(path-to-partial).mdx";

{/* Optional imports - only keep those you want to use in this guide:*/}
import { Callout } from "@stainless-api/docs/components"; - to add callout blocks. See src/components/Callout.astro for how to use it.
import { RelatedContentCards, RelatedContentCard } from "@components/RelatedContentCards"; - to add the Next steps section. See src/components/LearnMore.README.md for how to use it.
import ConnectionDetails from "@partials/_prereqs-connection-details.mdx"; - for integration guides that require entering connection details for a service or self-hosted database.
import { Image } from "astro:assets"; - for adding images. See https://docs.astro.build/en/guides/images/ for how to use it.
import { Tabs, TabItem } from "@astrojs/starlight/components"; - if you want to add tabs and provide separate steps for Tiger Cloud and self-hosted TimescaleDB. See https://starlight.astro.build/components/tabs/ for how to use it.


(One-line description of what the product does and why someone would use it.)

This page shows you how to integrate (Product name) with (Tiger Data product) to (specific outcome — what the reader gets at the end).

In this integration guide, you:

- (Outcome 1 — for example, configure your database for change data capture)
- (Outcome 2 — for example, capture row-level changes with Debezium)
- (Outcome 3 — for example, stream the captured changes to a Kafka topic)

<Prerequisites context="integration">
<IntegrationPrereqs />
{/* Optional:*/}
<ConnectionDetails />
- A [(Product name)](https://product-signup.com) account or installation, version X.Y or later.
- (Any other tooling, runtime, or credentials the reader needs — link to the install page for each.)
</Prerequisites>

## (Part one - what you achieve by the end of this section. Start with a verb. For example, "Configure your service to work with Debezium".)

(Context for this part of the integration.)

<NumberedList>
  <NumberedItem title="(Integration step 1)">

    (What to do.)

    (How to verify the result of this sub-step.)

    (Optionally an image to illustrate)

  </NumberedItem>
  <NumberedItem title="(Integration step 2)">

    (What to do.)

    (Optionally a code block)

    (How to verify the result of this sub-step.)

  </NumberedItem>
</NumberedList>

## (Part two — - what you achieve by the end of this section. Start with a verb. For example, "Configure Debezium to work with your service".)

(Follow the same logic as part 1)

## Verify the integration

To confirm (Product name) is working with your (Tiger Data product):

<NumberedList>
  <NumberedItem title='(Verification step 1 — for example, "Insert a row in your `accounts` table to trigger an event")'>

    (What to do.)

  </NumberedItem>
  <NumberedItem title='(Verification step 2 — for example, "Confirm Debezium published the event to the Kafka topic")'>

    (What to do.)

    You see (describe the expected rows/format/count so the reader knows what success looks like).

  </NumberedItem>
</NumberedList>

You have successfully integrated (Product name) with (Tiger Data product).

{/* Optional: troubleshooting — delete if there are no common gotchas worth calling out. Anything more general belongs in /integrate/troubleshooting. */}
## Troubleshooting

- **(Symptom — short error message or observable behavior):** (one-sentence cause and fix, or link to the relevant section).
- **(Second symptom):** (cause and fix).

For other connectivity and authentication issues, see [Troubleshoot {C.CLOUD_LONG} integrations](/integrate/troubleshooting).

{/* Optional: limitations — delete if there are none worth surfacing. */}
## Limitations

- (Feature, data type, or scenario that isn't supported, and the workaround if there is one.)
- (Version constraint, scale ceiling, or known incompatibility.)

{/* Optional: next steps — delete if there's no related material worth linking and only keep those cards that make sense */}
## Next steps

<RelatedContentCards>
  <RelatedContentCard
    title="(Related integration in the same category)"
    description="(One-sentence description)"
    href="/integrate/(category)/(slug)"
  />
  <RelatedContentCard
    title="(Concept or how-to this integration builds on)"
    description="(One-sentence description)"
    href="/learn/..."
  />
  <RelatedContentCard
    title="(Tutorial or example that uses this integration)"
    description="(One-sentence description)"
    href="/build/examples/..."
  />
</RelatedContentCards>