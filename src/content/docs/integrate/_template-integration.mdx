---
title: Integrate <third-party-product-name> with <tiger-data-product-name>
description: One-sentence description in sentence case for search and cards
integrationCategory: data-engineering-etl
integrationPlatforms: [aws, azure, self-hosted]
integrationIndustry: [crypto, iot, manufacturing]
keywords: [topic 1, topic 2, topic 3, topic 4, topic 5]
integrationCardLogo: product-name.png
sidebar:
  label: <Product name>
---

import * as C from "@constants";

import IntegrationPrereqs from '@partials/(_prereqs-*.mdx)';
import ConnectionDetails from '@partials/_prereqs-connection-details.mdx';
import { Prerequisites } from "@components/Prerequisites";
import { NumberedList, NumberedItem } from "@components/NumberedList";

[(Product name)](https://product-website.com) is a one-line description of what the product does.

This page shows you how to integrate (Product name) with (your {C.SERVICE_LONG} | your {C.SELF_LONG} instance | {C.TIMESCALE_DB}) to (specific outcome).

<Prerequisites context="integration">
<IntegrationPrereqs />
<ConnectionDetails />

- A [(Product name)](https://product-signup.com) account or installation.
</Prerequisites>

## (First setup task — start with a verb)

<NumberedList>
  <NumberedItem title="(Step 1 — start with a verb)">
    (Action the reader takes.) Use your [connection details](/integrate/find-connection-details) when filling in (host, port, etc.).
  </NumberedItem>
  <NumberedItem title="(Step 2)">
    ```sql
    -- example SQL
    ```
  </NumberedItem>
</NumberedList>

## (Second task — start with a verb)

<NumberedList>
  <NumberedItem title="(Step 1)">
    (Content.)
  </NumberedItem>
</NumberedList>

## Verify the integration

To confirm (Product name) is working with your database:

<NumberedList>
  <NumberedItem title="Connect to your database">
    Use the ({C.CONSOLE} | `psql` | any other SQL editor) to connect.
  </NumberedItem>
  <NumberedItem title="Check that data is present">
    ```sql
    SELECT * FROM <your_table> ORDER BY <time_column> DESC LIMIT 10;
    ```
  </NumberedItem>
</NumberedList>

You have successfully integrated (Product name) with ({C.CLOUD_LONG} | {C.TIMESCALE_DB}).