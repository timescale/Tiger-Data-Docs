---
title: "Integrate Debezium with TimescaleDB"
description: "Capture and stream database changes in real time for event-driven architecture"
integrationCategory: "data-engineering-etl"
integrationPlatforms: ["self-hosted"]
integrationIndustry: ["crypto", "healthcare", "manufacturing"]
keywords: ["change data capture","cdc","event streaming","database replication","debezium connector","real-time data","event-driven"]
integrationCardLogo: "debezium.png"
sidebar:
  label: "Debezium"
---

import * as C from "@constants";

import IntegrationDebeziumDocker from '@partials/_integration-debezium-docker.mdx';
import IntegrationDebeziumSelfHostedConfig from '@partials/_integration-debezium-self-hosted-config-database.mdx';
import IntegrationPrereqsSelfOnly from '@partials/_prereqs-self-instance.mdx';
import ConnectionDetails from '@partials/_prereqs-connection-details.mdx';
import { Prerequisites } from "@components/Prerequisites";

[Debezium](https://debezium.io/) is an open-source distributed platform for change data capture (CDC).
It enables you to capture changes in a {C.SELF_LONG} instance and stream them to other systems in real time.

Debezium can capture events about:

- [Hypertables](/learn/hypertables/understand-hypertables): captured events are rerouted from their chunk-specific topics to a single logical topic
  named according to the following pattern: `<topic.prefix>.<hypertable-schema-name>.<hypertable-name>`
- [Continuous aggregates](/learn/continuous-aggregates): captured events are rerouted from their chunk-specific topics to a single logical topic
  named according to the following pattern: `<topic.prefix>.<aggregate-schema-name>.<aggregate-name>`
- [Hypercore](/learn/columnar-storage/understand-hypercore): If you enable hypercore, the Debezium {C.TIMESCALE_DB} connector does not apply any special
  processing to data in the columnstore. Compressed chunks are forwarded unchanged to the next downstream job in the
  pipeline for further processing as needed. Typically, messages with compressed chunks are dropped, and are not
  processed by subsequent jobs in the pipeline.

  This limitation only affects changes to chunks in the columnstore. Changes to data in the rowstore work correctly.

This page explains how to capture changes in your database and stream them using Debezium on Apache Kafka.

<Prerequisites context="integration">
<IntegrationPrereqsSelfOnly />
<ConnectionDetails />

- [Docker](https://docs.docker.com/engine/install/) installed on your development machine.
</Prerequisites>

## Configure your database to work with Debezium

To set up {C.SELF_LONG} to communicate with Debezium:

<IntegrationDebeziumSelfHostedConfig />

## Configure Debezium to work with your database

Set up Kafka Connect server, plugins, drivers, and connectors:

<IntegrationDebeziumDocker />

And that is it, you have configured Debezium to interact with {C.TIMESCALE_DB}.
