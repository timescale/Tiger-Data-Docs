---
title: "Connect to Tiger Data"
description: "Connect to Tiger Cloud, self-hosted TimescaleDB, or MST using any PostgreSQL client"
products: [cloud, self_hosted]
keywords: ["connection string","database credentials","hostname","port","connection details","database connection","connection information"]
sidebar:
  label: "Connect to Tiger Data"
---

import * as C from "@constants";

import { Tabs, Tab } from '@stainless-api/docs/stainless-docs/mintlify-compat';
import { NumberedList, NumberedItem } from "@components/NumberedList";

import { Image } from "astro:assets";
import imgTigerServiceConnectionDetails from "../../../assets/images/integrate/tiger-service-connection-details.png";
import imgTigerConsoleChangeProject from "../../../assets/images/integrate/tiger-console-change-project.png";
import imgTigerConsoleServiceId from "../../../assets/images/integrate/tiger-console-service-id.png";
import imgTigerCloudConsoleClientCredentials from "../../../assets/images/integrate/tiger-cloud-console-client-credentials.png";
import imgMstConnectionInfo from "../../../assets/images/integrate/mst-connection-info.png";

To connect to your service or {C.SELF_LONG} instance, you need at least the following:

- Hostname
- Port
- Username
- Password
- Database name

Once you have these, connect using any {C.PG} client. Find the connection methods and details for your product:

<Tabs>
  <Tab title={C.CLOUD_LONG}>
    Connect to your {C.SERVICE_LONG} in any of these ways:

    - **{C.DATA_MODE} or {C.SQL_EDITOR} in {C.CONSOLE}**: run queries in your browser, with no local client required.
    - **`psql` or any other {C.PG} client**: use the connection details below with `psql`, or any {C.PG}-compatible client or SQL editor.

    Retrieve the connection details for your {C.SERVICE_LONG}:

    - **In `<service name>-credentials.txt`**:

      All connection details are supplied in the configuration file you download when you create a new {C.SERVICE_SHORT}.

    - **In {C.CONSOLE}**:

      Open the [`Services`](https://console.cloud.tigerdata.com/dashboard/services) page and select your {C.SERVICE_SHORT}. The connection details, except the password, are available in `Service info` > `Connection info` > `More details`. If necessary, click `Forgot your password?` to get a new one.

      <Image src={imgTigerServiceConnectionDetails} alt="Service connection details in Tiger Console" />

    ## Find your project and service ID

    To retrieve the connection details for your {C.PROJECT_LONG} and {C.SERVICE_LONG}:

    <NumberedList>
      <NumberedItem title={`Retrieve your ${C.PROJECT_SHORT} ID`}>
        In [{C.CONSOLE}](https://console.cloud.tigerdata.com/dashboard/services), click the arrows next to the {C.PROJECT_SHORT} name in the top left. The {C.PROJECT_SHORT} ID is under the {C.PROJECT_SHORT} name.

        <Image src={imgTigerConsoleChangeProject} alt="Retrieving the project ID in Tiger Console" />
      </NumberedItem>
      <NumberedItem title={`Retrieve your ${C.SERVICE_SHORT} ID`}>
        Click the dots next to the {C.SERVICE_SHORT}, then click `Copy` next to the {C.SERVICE_SHORT} ID.

        <Image src={imgTigerConsoleServiceId} alt="Retrieving the service ID in Tiger Console" />
      </NumberedItem>
    </NumberedList>

    ## Create client credentials

    You use client credentials to obtain access tokens outside of the user context.

    To retrieve the connection details for your {C.CLOUD_LONG} {C.PROJECT_SHORT} for programmatic usage
    such as Terraform or the [{C.CLOUD_LONG} REST API](/get-started/quickstart/rest-api):

    <NumberedList>
      <NumberedItem title={`Open the settings for your ${C.PROJECT_SHORT}`}>
        In [{C.CONSOLE}](https://console.cloud.tigerdata.com/dashboard/services), click `Settings`.
      </NumberedItem>
      <NumberedItem title="Create client credentials">
        1. Click `Create credentials`, then copy `Public key` and `Secret key` locally.

           <Image src={imgTigerCloudConsoleClientCredentials} alt="Creating client credentials in Tiger Console" />

           This is the only time you see the `Secret key`. After this, only the `Public key` is visible in this page.

        1. Click `Done`.
      </NumberedItem>
    </NumberedList>
  </Tab>

  <Tab title={C.SELF_LONG_CAP}>
    Connect using `psql` or any other {C.PG}-compatible client or SQL editor, with the connection details below.

    Find the connection details in the [{C.PG} configuration file](https://www.postgresql.org/docs/current/runtime-config-file-locations.html) or by asking your database administrator. The `postgres` superuser, created during {C.PG} installation, has all the permissions required to run procedures in this documentation. However, it is recommended to create other users and assign permissions on the need-only basis.
  </Tab>

  <Tab title={C.MST_LONG}>
    Connect using `psql` or any other {C.PG}-compatible client, with the connection details below.

    In the `Services` page of the {C.MST_CONSOLE_LONG}, click the {C.MST_SERVICE_SHORT} you want to connect to. You see the connection details:

    <Image src={imgMstConnectionInfo} alt="MST service connection details" />
  </Tab>
</Tabs>
