---
title: "Analyze transport and geospatial data"
description: "Simulate and analyze a transport dataset in Tiger Cloud"
products: [cloud, self_hosted]
keywords: [transport, geospatial, real-time analytics, taxi data, Grafana, visualization, time-series]
---

import * as C from "@constants";
import { NumberedList, NumberedItem } from "@components/NumberedList";
import CreateHypertableNYCTaxis from "../../../../partials/_create-hypertable-nyctaxis.mdx";
import GeolocationAnalytics from "../../../../partials/_use-case-transport-geolocation.mdx";
import GrafanaConnect from "../../../../partials/_grafana-connect.mdx";
import IntegrationPrereqs from "../../../../partials/_prereqs-cloud-and-self.mdx";
import { Prerequisites } from "@components/Prerequisites";

import { Image } from "astro:assets";
import imgUseCaseRtaGrafanaHeatmap from "../../../../assets/images/build/examples/use-case-rta-grafana-heatmap.png";
import imgUseCaseRtaGrafanaTimescaleConfigureDashboard from "../../../../assets/images/build/examples/use-case-rta-grafana-timescale-configure-dashboard.png";
import imgUseCaseRtaGrafanaTimescaleFinalDashboard from "../../../../assets/images/build/examples/use-case-rta-grafana-timescale-final-dashboard.png";

This walkthrough uses NYC taxi-style geospatial time series in {C.SERVICE_LONG}, then layers Grafana on top so you can see movement and hotspots as they change instead of after the fact.

<Image src={imgUseCaseRtaGrafanaHeatmap} alt="Grafana heatmap showing NYC taxi trip pickup locations over time" />

This page shows you how to integrate [Grafana](https://grafana.com/docs/) with a {C.SERVICE_LONG} and make insights based on visualization
of data optimized for size and speed in the {C.COLUMNSTORE}.

<Prerequisites intro={false}>
<IntegrationPrereqs />

* Install and run [self-managed Grafana](https://grafana.com/get/?tab=self-managed), or sign up for [Grafana Cloud](https://grafana.com/get/).
</Prerequisites>

<CreateHypertableNYCTaxis />

<GrafanaConnect />

## Monitor performance over time

A Grafana dashboard represents a view into the performance of a system, and each dashboard consists of one or
more panels, which represent information about a specific metric related to that system.

To visually monitor the volume of taxi rides over time:

<NumberedList>
<NumberedItem title="Create the dashboard">

1. On the `Dashboards` page, click `New` and select `New dashboard`.

1. Click `Add visualization`.

1. Select the data source that connects to your {C.SERVICE_LONG}.
   The `Time series` visualization is chosen by default.

   <Image src={imgUseCaseRtaGrafanaTimescaleConfigureDashboard} alt="Configuring a Grafana dashboard with a TimescaleDB data source" />

1. In the `Queries` section, select `Code`, then select `Time series` in `Format`.

1. Select the data range for your visualization:
   the data set is from 2016. Click the date range above the panel and set:

   - From: `2016-01-01 01:00:00`
   - To: `2016-01-30 01:00:00`

</NumberedItem>
<NumberedItem title={`Combine ${C.TIMESCALE_DB} and Grafana functionality to analyze your data`}>

Combine a {C.TIMESCALE_DB} [time_bucket](/learn/data-lifecycle/time-buckets/about-time-buckets), with the Grafana `$__timefilter()` function to set the
`pickup_datetime` column as the filtering range for your visualizations.

```sql
SELECT
  time_bucket('1 day', pickup_datetime) AS "time",
  COUNT(*)
FROM rides
WHERE $__timeFilter(pickup_datetime)
GROUP BY time
ORDER BY time;
```

This query groups the results by day and orders them by time.

<Image src={imgUseCaseRtaGrafanaTimescaleFinalDashboard} alt="Completed Grafana dashboard showing NYC taxi ride count and geospatial data" />

</NumberedItem>
<NumberedItem title="Click `Save dashboard`">
</NumberedItem>
</NumberedList>

## Optimize revenue potential

Having all this data is great but how do you use it? Monitoring data is useful to check what
has happened, but how can you analyse this information to your advantage? This section explains
how to create a visualization that shows how you can maximize potential revenue.

<GeolocationAnalytics />

You have integrated Grafana with a {C.SERVICE_LONG} and made insights based on visualization of
your data.