---
title: Upload a file using Tiger Console
description: Upload CSV, Parquet, and text files into your Tiger Cloud service using Tiger Console. Drag and drop from your local machine, or provide a path to the file in S3
---

import * as C from "@constants";
import { Tabs, TabItem } from "@astrojs/starlight/components";
import { Badge, Callout } from "@stainless-api/docs/components";
import { FlaskConicalIcon } from 'lucide-react';
import { NumberedList, NumberedItem } from "@components/NumberedList";

import ImportPrerequisitesCloudNoConnection from "../../../partials/_prereqs-cloud-no-connection.mdx";
import NotSupportedAzure from "../../../partials/_not-supported-for-azure.mdx";

import { Image } from "astro:assets";
import imgTigerConsoleImportCsvFile from "../../../assets/images/migrate/tiger-console-import-csv-file.png";
import imgTigerConsoleConfigureCsvFileImport from "../../../assets/images/migrate/tiger-console-configure-csv-file-import.png";
import imgTigerConsoleImportParquetFile from "../../../assets/images/migrate/tiger-console-import-parquet-file.png";
import imgTigerConsoleConfigureParquetFileImport from "../../../assets/images/migrate/tiger-console-configure-parquet-file-import.png";
import imgTigerConsoleImportTxtFile from "../../../assets/images/migrate/tiger-console-import-txt-file.png";
import imgTigerConsoleConfigureTxtFileImport from "../../../assets/images/migrate/tiger-console-configure-txt-file-import.png";
import imgImportCsvFileFromS3 from "../../../assets/images/migrate/import-csv-file-from-s3.png";
import imgConfigureCsvFileImportFromS3 from "../../../assets/images/migrate/configure-csv-file-import-from-s3.png";
import imgTigerCloudImportParquetFileFromS3 from "../../../assets/images/migrate/tiger-cloud-import-parquet-file-from-s3.png";

<Badge intent="accent" size="md">
  <FlaskConicalIcon slot="icon" />
  Early access
</Badge>

You can upload files into your {C.SERVICE_SHORT} using {C.CONSOLE_LONG}. This page explains how to upload CSV, Parquet, and text files, from your local machine and from an S3 bucket.

<Tabs>

<TabItem label="From a local machine">

{C.CONSOLE_LONG} enables you to drag and drop files to upload from your local machine.

## Prerequisites

<ImportPrerequisitesCloudNoConnection />

<NotSupportedAzure />

<Tabs>

<TabItem label="From CSV">

To upload a CSV file to your {C.SERVICE_SHORT}:

<NumberedList>

<NumberedItem title={`Select your ${C.SERVICE_SHORT} in ${C.CONSOLE_SHORT}, then click \`Actions\` > \`Import data\` > \`Upload your files\` > \`Upload CSV file\``}>

<Image src={imgTigerConsoleImportCsvFile} alt="Uploading a CSV file in Tiger Console" />

</NumberedItem>

<NumberedItem title="Click to browse, or drag the file to import">
</NumberedItem>

<NumberedItem title="Configure the import">

<Image src={imgTigerConsoleConfigureCsvFileImport} alt="Configuring CSV file import settings in Tiger Console" />

- Set a delimiter.
- Toggle to skip or keep the header.
- Select to ingest the data into an existing table or create a new one.
- Provide the new or existing table name.
- (Optional) For a new table with a time column, toggle the time column to create a hypertable instead of a regular table.

</NumberedItem>

<NumberedItem title="Click `Process CSV file`">

When the processing is completed, to find the data your imported, click `Explorer`.

</NumberedItem>

</NumberedList>

</TabItem>

<TabItem label="From Parquet">

To upload a Parquet file to your {C.SERVICE_SHORT}:

<NumberedList>

<NumberedItem title={`Select your ${C.SERVICE_SHORT} in ${C.CONSOLE_SHORT}, then click \`Actions\` > \`Import data\` > \`Upload your files\` > \`Upload Parquet file\``}>

<Image src={imgTigerConsoleImportParquetFile} alt="Uploading a Parquet file in Tiger Console" />

</NumberedItem>

<NumberedItem title="Click to browse, or drag the file to import">
</NumberedItem>

<NumberedItem title="Configure the import">

<Image src={imgTigerConsoleConfigureParquetFileImport} alt="Configuring Parquet file import settings in Tiger Console" />

- Select to ingest the data into an existing table or create a new one.
- Provide the new or existing table name.
- (Optional) For a new table with a time column, toggle the time column to create a hypertable instead of a regular table.

</NumberedItem>

<NumberedItem title="Click `Process Parquet file`">

When the processing is completed, to find the data your imported, click `Explorer`.

</NumberedItem>

</NumberedList>

</TabItem>

<TabItem label="From a text file">

To upload a TXT or MD file to your {C.SERVICE_SHORT}:

<NumberedList>

<NumberedItem title={`Select your ${C.SERVICE_SHORT} in ${C.CONSOLE_SHORT}, then click \`Actions\` > \`Import data\` > \`Upload your files\` > \`Upload Text file\``}>

<Image src={imgTigerConsoleImportTxtFile} alt="Uploading a text file in Tiger Console" />

</NumberedItem>

<NumberedItem title="Click to browse, or drag and drop the file to import">
</NumberedItem>

<NumberedItem title="Configure the import">

Provide a name to create a new table, or select an existing table to add data to.

<Image src={imgTigerConsoleConfigureTxtFileImport} alt="Configuring text file import settings in Tiger Console" />

</NumberedItem>

<NumberedItem title="Click `Upload files`">

When the upload is finished, find your data imported to a new or existing table in `Explorer`.

</NumberedItem>

</NumberedList>

</TabItem>

</Tabs>

</TabItem>

<TabItem label="From S3">

{C.CONSOLE_LONG} enables you to upload CSV and Parquet files, including archives compressed using GZIP and ZIP, by connecting to an S3 bucket.

## Prerequisites

<ImportPrerequisitesCloudNoConnection />

- Ensure access to a standard Amazon S3 bucket containing your data files.
- Configure access credentials for the S3 bucket. The following credentials are supported:
   - [IAM Role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_create_for-user.html#roles-creatingrole-user-console).
   - [Public anonymous user](https://docs.aws.amazon.com/AmazonS3/latest/userguide/example-bucket-policies.html#example-bucket-policies-anonymous-user).

<NotSupportedAzure />

<Tabs>

<TabItem label="From CSV">

To import a CSV file from an S3 bucket:

<NumberedList>

<NumberedItem title={`Select your ${C.SERVICE_SHORT} in ${C.CONSOLE_SHORT}, then click \`Actions\` > \`Import data\` > \`Explore import options\` > \`Import from S3\``}>
</NumberedItem>

<NumberedItem title="Select your file in the S3 bucket">

<Image src={imgImportCsvFileFromS3} alt="Importing a CSV file from S3 in Tiger Console" />

1. Provide your file path.
1. Select `CSV` in the file type dropdown.
1. Select the authentication method:
   - `IAM role` and provide the role.
   - `Public`.
1. Click `Continue`.

</NumberedItem>

<NumberedItem title="Configure the import">

<Image src={imgConfigureCsvFileImportFromS3} alt="Configuring CSV import from S3 in Tiger Console" />

- Set a delimiter.
- Toggle to skip or keep the header.
- Select to ingest the data into an existing table or create a new one.
- Provide the new or existing table name.
- (Optional) For a new table with a time column, toggle the time column to create a hypertable instead of a regular table.

</NumberedItem>

<NumberedItem title="Click `Process CSV file`">

When the processing is completed, to find the data your imported, click `Explorer`.

</NumberedItem>

</NumberedList>

</TabItem>

<TabItem label="From Parquet">

To import a Parquet file from an S3 bucket:

<NumberedList>

<NumberedItem title={`Select your ${C.SERVICE_SHORT} in ${C.CONSOLE_SHORT}, then click \`Actions\` > \`Import from S3\``}>
</NumberedItem>

<NumberedItem title="Select your file in the S3 bucket">

<Image src={imgTigerCloudImportParquetFileFromS3} alt="Importing a Parquet file from S3 in Tiger Console" />

1. Provide your file path.
1. Select `Parquet` in the file type dropdown.
1. Select the authentication method:
   - `IAM role` and provide the role.
   - `Public`.
1. Click `Continue`.

</NumberedItem>

<NumberedItem title="Configure the import">

- Select `Create a new table for your data` or `Ingest data to an existing table`.
- Provide the new or existing table name.
- (Optional) For a new table with a time column, toggle the time column to create a hypertable instead of a regular table.

</NumberedItem>

<NumberedItem title="Click `Process Parquet file`">

When the processing is completed, to find the data your imported, click `Explorer`.

</NumberedItem>

</NumberedList>

</TabItem>

</Tabs>

</TabItem>

</Tabs>

And that is it, you have imported your data to your {C.SERVICE_LONG}.

## Troubleshooting

- **Upload fails or times out:** Use a smaller file or split the data. For large or automated loads, use [import via terminal](/migrate/import-terminal).
- **Column mapping errors:** Ensure headers match the expected table columns, including case.
- **Data type or null errors:** Check that values fit the column types and that NOT NULL columns have no empty values, or adjust null handling in the import settings.
- **Encoding issues:** Save the file as UTF-8 to avoid corrupted characters.

## Related

- [Upload a file using the terminal](/migrate/import-terminal): Use `psql`, `COPY`, or `pg_restore` for large or automated imports.
- [Sync from S3](/migrate/livesync-for-s3): Continuously sync CSV and Parquet from an S3 bucket.
- [Sync data from {C.PG}](/migrate/livesync-for-postgresql): Continuously replicate from a {C.PG} database.
- [Live import from a database](/migrate/live-migration): Migrate with minimal or no downtime.
- [Migrate overview](/migrate): All import and migration options.