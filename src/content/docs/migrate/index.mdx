---
title: Migrate to Tiger Data
description: Move your data to TimescaleDB or Tiger Cloud from other databases
overviewDescription: Import methods and database-specific migration guides
---

import { RelatedContentCards, RelatedContentCard } from "@components/RelatedContentCards";
import * as C from "@constants";

Pick an **import method** (how you run the migration) or a **source-specific guide** (step-by-step from your current database).

## Import methods

Choose how you run the migration—whether you prefer the {C.CLOUD_LONG} UI, the command line, or a strategy that fits your downtime window.

<RelatedContentCards>
  <RelatedContentCard title={`Sync from ${C.PG}`} description={`Continuously replicate ${C.PG} tables to ${C.CLOUD_LONG} in real time using the ${C.PG_CONNECTOR} (livesync).`} href="/migrate/livesync-for-postgresql" />
  <RelatedContentCard title="Sync from S3" description={`Synchronize CSV and Parquet files from an S3 bucket to ${C.CLOUD_LONG} in real time using the ${C.S3_CONNECTOR}.`} href="/migrate/livesync-for-s3" />
  <RelatedContentCard title="Upload a file using the Console" description={`Upload CSV, Parquet, and text files from your machine or S3 via ${C.CONSOLE}—no ${C.CLI_SHORT} required.`} href="/migrate/import-console" />
  <RelatedContentCard title="Upload a file using the terminal" description={`Upload CSV, MySQL, and Parquet files from your machine into ${C.CLOUD_LONG} using the terminal (psql, COPY, and migration tools).`} href="/migrate/import-terminal" />
  <RelatedContentCard title="Live migration" description="Migrate with minimal or no downtime using replication and sync." href="/migrate/live-migration" />
  <RelatedContentCard title="Migrate with downtime" description={`Standard migration with a maintenance window: export from source, import to ${C.COMPANY}.`} href="/migrate/migrate-with-downtime" />
</RelatedContentCards>

## Migrate from a specific database

Guides tailored to your current database: schema mapping, tooling, and gotchas for each source.

<RelatedContentCards>
  <RelatedContentCard title={C.PG} description={`Move from vanilla ${C.PG} to Tiger Data with minimal schema changes.`} href="/migrate/migrate-from/postgres" />
  <RelatedContentCard title="MongoDB" description={`Migrate documents and collections into ${C.COMPANY}’s relational model.`} href="/migrate/migrate-from/mongodb" />
  <RelatedContentCard title="ClickHouse" description={`Move time-series and analytical workloads from ClickHouse to ${C.COMPANY}.`} href="/migrate/migrate-from/clickhouse" />
  <RelatedContentCard title="All migration sources" description="Browse by source (MySQL, InfluxDB, CockroachDB, and more)." href="/migrate/migrate-from" />
</RelatedContentCards>
