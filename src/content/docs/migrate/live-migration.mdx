---
title: Live migration (deprecated)
description: Migrate your entire database to Tiger Cloud with low downtime
products: [cloud]
learnMore:
  relatedPostsHeading: Related resources
  relatedPosts:
    - label: "Video: migrate from MongoDB to Tiger Data in 2 steps with Claude Code"
      href: https://www.youtube.com/watch?v=nkuOXxxXivU
    - label: "Video: BioT Medical's migration to Tiger Data, from 5s queries to milliseconds"
      href: https://www.youtube.com/watch?v=Jg4_Ik1-HLg
deprecated: true
pageLabels: [Deprecated]
---

import * as C from "@constants";
import { Tabs, TabItem } from "@astrojs/starlight/components";
import { Callout } from "@stainless-api/docs/components";
import MigrationPrerequisites from "../../../partials/_migrate_prerequisites.mdx";
import { Prerequisites } from "@components/Prerequisites";
import DoNotRecommendForLargeMigration from "../../../partials/_migrate_pg_dump_do_not_recommend_for_large_migration.mdx";
import MigrateSetupEnvironment from "../../../partials/_migrate_live_setup_environment.mdx";
import MigrateSetupEnvironmentPostgres from "../../../partials/_migrate_live_setup_environment_postgres.mdx";
import MigrateSetupEnvironmentMST from "../../../partials/_migrate_live_setup_environment_mst.mdx";
import MigrateSetupEnvironmentAWSRDS from "../../../partials/_migrate_live_setup_environment_awsrds.mdx";
import MigrateDataToCloud from "../../../partials/_migrate_live_migrate_data.mdx";
import MigrateDataToCloudTimescaleDB from "../../../partials/_migrate_live_migrate_data_timescaledb.mdx";
import ValidateDataInCloud from "../../../partials/_migrate_live_validate_data.mdx";
import MigrateAWSRDSConnectIntermediary from "../../../partials/_migrate_awsrds_connect_intermediary.mdx";
import Troubleshooting from "../../../partials/_migrate_live_migrate_faq_all.mdx";

<Callout variant="important">
Live migration is deprecated. For new migrations, use [Livesync replication](/migrate/livesync-replication), which provides the same near-zero-downtime experience with better throughput and broader source-database support.
</Callout>

Live migration is an end-to-end solution that copies the database schema and data to
your target {C.SERVICE_LONG}, then replicates the database activity in your source database to the target service in real time. Live migration uses the {C.PG} logical decoding functionality and leverages [pgcopydb](https://pgcopydb.readthedocs.io/).

You use the live migration Docker image to move 100GB-10TB+ of data to a {C.SERVICE_LONG} seamlessly with only a few minutes downtime.

<DoNotRecommendForLargeMigration />

Best practice is to use live migration when:
- Modifying your application logic to perform dual writes is a significant effort.
- The insert workload does not exceed 20,000 rows per second, and inserts are batched.
- Your source database:
  - Uses `UPDATE` and `DELETE` statements on uncompressed time-series data.

    Live migration does not support replicating `INSERT`/`UPDATE`/`DELETE` statements on compressed data.
  - Has large, busy tables with primary keys.
  - Does not have many `UPDATE` or `DELETE` statements.

This page shows you how to move your data from a {C.SELF_SHORT} database to a {C.SERVICE_LONG} using
the live-migration Docker image.

<Prerequisites context="procedure">
<MigrationPrerequisites />

- [Docker](https://docs.docker.com/engine/install/) installed on your migration machine.

  This machine needs sufficient space to store the buffered changes that occur while your data is
  being copied. This space is proportional to the amount of new uncompressed data being written to
  the {C.SERVICE_LONG} during migration. A general rule of thumb is between 100GB and 500GB.
  The CPU specifications of this EC2 instance should match those of your {C.SERVICE_LONG} for optimal performance. For example, if your {C.SERVICE_SHORT} has an 8-CPU configuration, then your EC2 instance should also have 8 CPUs.

- Familiarity with the [troubleshooting section](#troubleshooting) before starting live migration.
</Prerequisites>

## Migrate to {C.CLOUD_LONG}

To move your data from a {C.SELF_SHORT} database to a {C.SERVICE_LONG}:

<Tabs>
<TabItem label="From TimescaleDB">

This section shows you how to move your data from {C.SELF_LONG} to a {C.SERVICE_LONG}
using live migration from Terminal.

<MigrateSetupEnvironment />

<MigrateDataToCloudTimescaleDB />

<ValidateDataInCloud />

</TabItem>
<TabItem label={`From ${C.PG}`}>

This section shows you how to move your data from {C.SELF_SHORT} {C.PG} to a {C.SERVICE_LONG} using
live migration from Terminal.

<MigrateSetupEnvironmentPostgres />

<MigrateDataToCloud />

<ValidateDataInCloud />

</TabItem>
<TabItem label="From AWS RDS/Aurora">

To migrate your data from an Amazon RDS/Aurora {C.PG} instance to a {C.SERVICE_LONG}, you extract the data to an intermediary
EC2 Ubuntu instance in the same AWS region as your RDS/Aurora instance. You then upload your data to a {C.SERVICE_LONG}.
To make this process as painless as possible, ensure that the intermediary machine has enough CPU and disk space to
rapidly extract and store your data before uploading to {C.CLOUD_LONG}.

Migration from RDS/Aurora gives you the opportunity to create {C.HYPERTABLE}s before copying the data. Once the migration is complete, you can manually enable {C.CLOUD_LONG} features like [data compression](/learn/columnar-storage/understand-hypercore) or [data retention](/learn/data-lifecycle/data-retention/about-data-retention).

This section shows you how to move your data from an Amazon RDS/Aurora instance to a {C.SERVICE_LONG}
using live migration.

<MigrateAWSRDSConnectIntermediary />

<MigrateSetupEnvironmentAWSRDS />

<MigrateDataToCloud />

<ValidateDataInCloud />

</TabItem>
<TabItem label="From MST">

This section shows you how to move your data from a {C.MST_SHORT} instance to a
{C.SERVICE_LONG} using live migration from Terminal.

<MigrateSetupEnvironmentMST />

<MigrateDataToCloudTimescaleDB />

<ValidateDataInCloud />

</TabItem>
</Tabs>

And you are done, your data is now in your {C.SERVICE_LONG}.

## Troubleshooting

This section shows you how to work around frequently seen issues when using live migration.

<Troubleshooting />
