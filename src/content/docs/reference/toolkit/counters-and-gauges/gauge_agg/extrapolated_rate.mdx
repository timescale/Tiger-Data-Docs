---
title: extrapolated_rate()
description: Calculate the extrapolated rate of change from a gauge aggregate
keywords: [counters and gauges, extrapolated_rate, gauge agg, timescaledb toolkit]
---

import SinceRelease from '@components/SinceRelease.astro';

<SinceRelease version="1.6.0" product="toolkit" mode="early_access" />

Calculate the rate of change of a gauge during the time period specified by the bounds
in the gauge aggregate. The bounds must be specified for the `extrapolated_rate`
function to work. You can provide them as part of the original [`gauge_agg`](/reference/toolkit/counters-and-gauges/gauge_agg/gauge_agg)
call, or by using the [`with_bounds`](/reference/toolkit/counters-and-gauges/gauge_agg/with_bounds) function on an existing
gauge aggregate.

## Samples

```sql
SELECT
    id,
    bucket,
    extrapolated_rate(
        with_bounds(
            summary,
            toolkit_experimental.time_bucket_range('15 min'::interval, bucket)
        )
    )
FROM (
    SELECT
        id,
        time_bucket('15 min'::interval, ts) AS bucket,
        gauge_agg(ts, val) AS summary
    FROM foo
    GROUP BY id, time_bucket('15 min'::interval, ts)
) t;
```

## Arguments

The syntax is:

```sql
extrapolated_rate(
    summary GaugeSummary
) RETURNS DOUBLE PRECISION
```

| Name | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `summary` | `GaugeSummary` | - | ✔ | A gauge aggregate created using [`gauge_agg`](/reference/toolkit/counters-and-gauges/gauge_agg/gauge_agg) |

## Returns

| Column | Type | Description |
|--------|------|-------------|
| extrapolated\_rate | DOUBLE PRECISION | The extrapolated rate of change of the gauge over the time period of the gauge aggregate. |
