---
title: rollup()
description: Combine multiple gauge aggregates
products: [cloud, self_hosted]
keywords: [counters and gauges, gauge agg, rollup, timescaledb toolkit]
---

import SinceRelease from '@components/SinceRelease.astro';

<SinceRelease version="1.6.0" product="toolkit" mode="early_access" />

This function combines multiple gauge aggregates into one. This can be used to combine aggregates from adjacent
intervals into one larger interval, such as rolling daily aggregates into a weekly or monthly aggregate.

## Arguments

The syntax is:

```sql
rollup(
    gs GaugeSummary
) RETURNS GaugeSummary
```

| Name | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `gs` | `GaugeSummary` | - | ✔ | A gauge aggregate created using [`gauge_agg`](/reference/toolkit/counters-and-gauges/gauge_agg/gauge_agg) |

## Returns

| Column | Type | Description |
|--------|------|-------------|
| rollup | GaugeSummary | A new gauge aggregate created by combining the input gauge aggregates |
