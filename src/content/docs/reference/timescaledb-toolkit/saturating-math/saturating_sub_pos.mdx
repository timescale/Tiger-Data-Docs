---
title: saturating_sub_pos()
description: Subtract one number from another, saturating at 0 for the minimum bound
topics: [hyperfunctions]
license: community
type: function
toolkit: true
products: [cloud, mst, self_hosted]
hyperfunction:
  family: saturating math
  type: function
---

import { FlaskConicalIcon, TagIcon } from 'lucide-react';

<FlaskConicalIcon /> Early access <TagIcon iconType="duotone" /> Since [1.8.0][toolkit-1.8.0]

The `saturating_sub_pos` function subtracts the second number from the first, saturating at 0 and 2,147,483,647 instead
of overflowing.

## Arguments

The syntax is:

```sql
saturating_sub_pos(
  x INT,
  y INT
) RETURNS INT
```

| Name | Type | Default | Required | Description |
|------|------|---------|----------|-------------|
| x | INT | - | ✔ | An integer for `y` to subtract from |
| y | INT | - | ✔ | An integer to subtract from `x` |

## Returns

| Column | Type | Description |
|--------|------|-------------|
| saturating\_sub\_pos | INT | The result of `x - y`, saturating at 0 for the minimum bound |

[toolkit-1.8.0]: https://github.com/timescale/timescaledb-toolkit/releases/tag/1.8.0
