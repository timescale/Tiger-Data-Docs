---
title: uuid_version()
description: Extract the version of a UUID
products: [cloud, self_hosted]
keywords: [uuid]
---

import SinceRelease from '@components/SinceRelease.astro';

import ThemeImage from "@components/ThemeImage.astro";
import imgUuidv7StructureLight from "../../../../../assets/images/reference/timescaledb/uuidv7-structure.png";
import imgUuidv7StructureDark from "../../../../../assets/images/reference/timescaledb/uuidv7-structure-dark.png";

<SinceRelease version="2.23.0" product="tsdb" />

Extract the version number from a UUID object:

<ThemeImage light={imgUuidv7StructureLight} dark={imgUuidv7StructureDark} alt="UUIDv7 structure: timestamp, version, random bits" />

## Samples

```sql
SELECT uuid_version('019913ce-f124-7835-96c7-a2df691caa98');
```

Returns something like:

```txt
 uuid_version
--------------
            7
```

## Arguments

The syntax is:

```sql
SELECT uuid_version(
    uuid = <uuid>
);
```

| Name | Type | Default | Required | Description |
| - | - | - | - | - |
| `uuid` | UUID | - | ✔ | The UUID object to extract the version number from |

## Returns

|Column|Type|Description|
|-|-|-|
| `uuid_version` | INTEGER | The version number extracted from the UUID object (e.g., 7 for UUIDv7). |
